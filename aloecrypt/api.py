# This file is auto-generated by generate_py.py. Do not edit manually.

from __future__ import annotations
from typing import Optional, NewType
from aloecrypt._plugin import _PluginModel

# --- Size Constants ---

class consts_api:
    PEM_CHUNK_SZ: int = 32
    HASH_SZ: int = 32
    ADDRESS_SZ: int = 32
    VERIFY_KEY_SZ: int = 1952
    SIGN_KEY_SZ: int = 4032
    SIGN_SEED_SZ: int = 32
    SIGNATURE_SZ: int = 3309
    ENCAPSULATE_KEY_SZ: int = 1184
    DECAPSULATE_KEY_SZ: int = 2400
    KYBER_SEED_SZ: int = 64
    CIPHER_SZ: int = 1088
    SECRET_SZ: int = 32
    TIMESTAMP_SZ: int = 8
    CHACHA_NONCE_SZ: int = 12
    CHACHA_KEY_SZ: int = 32
    SESSION_NONCE_SZ: int = 32
    SESSION_SALT_SZ: int = 32
    ENCRYPTED_TAG_SZ: int = 16
    ENCRYPTED_SESSION_SALT_SZ: int = SESSION_SALT_SZ + ENCRYPTED_TAG_SZ
    ENCRYPTED_SIGN_KEY_SZ: int = SIGN_KEY_SZ + ENCRYPTED_TAG_SZ
    ENCRYPTED_SIGN_SEED_SZ: int = SIGN_SEED_SZ + ENCRYPTED_TAG_SZ
    ENCRYPTED_DECAPSULATE_KEY_SZ: int = DECAPSULATE_KEY_SZ + ENCRYPTED_TAG_SZ
    ENCRYPTED_NONCE_SZ: int = SESSION_NONCE_SZ + 2 * ENCRYPTED_TAG_SZ

# Flat const references for use in type aliases
PEM_CHUNK_SZ = consts_api.PEM_CHUNK_SZ
HASH_SZ = consts_api.HASH_SZ
ADDRESS_SZ = consts_api.ADDRESS_SZ
VERIFY_KEY_SZ = consts_api.VERIFY_KEY_SZ
SIGN_KEY_SZ = consts_api.SIGN_KEY_SZ
SIGN_SEED_SZ = consts_api.SIGN_SEED_SZ
SIGNATURE_SZ = consts_api.SIGNATURE_SZ
ENCAPSULATE_KEY_SZ = consts_api.ENCAPSULATE_KEY_SZ
DECAPSULATE_KEY_SZ = consts_api.DECAPSULATE_KEY_SZ
KYBER_SEED_SZ = consts_api.KYBER_SEED_SZ
CIPHER_SZ = consts_api.CIPHER_SZ
SECRET_SZ = consts_api.SECRET_SZ
TIMESTAMP_SZ = consts_api.TIMESTAMP_SZ
CHACHA_NONCE_SZ = consts_api.CHACHA_NONCE_SZ
CHACHA_KEY_SZ = consts_api.CHACHA_KEY_SZ
SESSION_NONCE_SZ = consts_api.SESSION_NONCE_SZ
SESSION_SALT_SZ = consts_api.SESSION_SALT_SZ
ENCRYPTED_TAG_SZ = consts_api.ENCRYPTED_TAG_SZ
ENCRYPTED_SESSION_SALT_SZ = consts_api.ENCRYPTED_SESSION_SALT_SZ
ENCRYPTED_SIGN_KEY_SZ = consts_api.ENCRYPTED_SIGN_KEY_SZ
ENCRYPTED_SIGN_SEED_SZ = consts_api.ENCRYPTED_SIGN_SEED_SZ
ENCRYPTED_DECAPSULATE_KEY_SZ = consts_api.ENCRYPTED_DECAPSULATE_KEY_SZ
ENCRYPTED_NONCE_SZ = consts_api.ENCRYPTED_NONCE_SZ

# --- Byte Type Aliases ---

DilithiumPubkey = NewType('DilithiumPubkey', bytes)
DilithiumPrivkey = NewType('DilithiumPrivkey', bytes)
XDilithiumPrivkey = NewType('XDilithiumPrivkey', bytes)
DilithiumPrivSeed = NewType('DilithiumPrivSeed', bytes)
XDilithiumPrivSeed = NewType('XDilithiumPrivSeed', bytes)
KyberPubkey = NewType('KyberPubkey', bytes)
KyberPrivkey = NewType('KyberPrivkey', bytes)
XKyberPrivkey = NewType('XKyberPrivkey', bytes)
KyberSeed = NewType('KyberSeed', bytes)
AloecryptAddress = NewType('AloecryptAddress', bytes)
AloecryptHash = NewType('AloecryptHash', bytes)
AloecryptSecret = NewType('AloecryptSecret', bytes)
XAloecryptSecret = NewType('XAloecryptSecret', bytes)
AloecryptSessionNonce = NewType('AloecryptSessionNonce', bytes)
XAloecryptSessionNonce = NewType('XAloecryptSessionNonce', bytes)
AloecryptSessionSalt = NewType('AloecryptSessionSalt', bytes)
XAloecryptSessionSalt = NewType('XAloecryptSessionSalt', bytes)
Timestamp = NewType('Timestamp', bytes)
KyberCipher = NewType('KyberCipher', bytes)
DilithiumSignature = NewType('DilithiumSignature', bytes)
ChachaNonce = NewType('ChachaNonce', bytes)
ChachaKey = NewType('ChachaKey', bytes)

# --- Models ---

class kem_api:
    class KyberFullKEM(_PluginModel):
        kyb_pubkey: KyberPubkey
        kyb_privkey: KyberPrivkey
        kyb_sig_bytes: DilithiumSignature
        dlt_auth_pubkey: DilithiumPubkey
        dlt_root_pubkey: DilithiumPubkey
        dlt_root_address: AloecryptAddress
        dlt_auth_address: AloecryptAddress
        dlt_created_at: Timestamp
        dlt_active_from: Timestamp
        dlt_expires_at: Timestamp
        dlt_refresh_count: int
        dlt_max_refresh: int
        dlt_generation: int

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 10901

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(10901)
            off = 0
            assert len(self.kyb_pubkey) == 1184, f'kyb_pubkey must be 1184 bytes, got {len(self.kyb_pubkey)}'
            buf.extend(self.kyb_pubkey)
            assert len(self.kyb_privkey) == 2400, f'kyb_privkey must be 2400 bytes, got {len(self.kyb_privkey)}'
            buf.extend(self.kyb_privkey)
            assert len(self.kyb_sig_bytes) == 3309, f'kyb_sig_bytes must be 3309 bytes, got {len(self.kyb_sig_bytes)}'
            buf.extend(self.kyb_sig_bytes)
            assert len(self.dlt_auth_pubkey) == 1952, f'dlt_auth_pubkey must be 1952 bytes, got {len(self.dlt_auth_pubkey)}'
            buf.extend(self.dlt_auth_pubkey)
            assert len(self.dlt_root_pubkey) == 1952, f'dlt_root_pubkey must be 1952 bytes, got {len(self.dlt_root_pubkey)}'
            buf.extend(self.dlt_root_pubkey)
            assert len(self.dlt_root_address) == 32, f'dlt_root_address must be 32 bytes, got {len(self.dlt_root_address)}'
            buf.extend(self.dlt_root_address)
            assert len(self.dlt_auth_address) == 32, f'dlt_auth_address must be 32 bytes, got {len(self.dlt_auth_address)}'
            buf.extend(self.dlt_auth_address)
            assert len(self.dlt_created_at) == 8, f'dlt_created_at must be 8 bytes, got {len(self.dlt_created_at)}'
            buf.extend(self.dlt_created_at)
            assert len(self.dlt_active_from) == 8, f'dlt_active_from must be 8 bytes, got {len(self.dlt_active_from)}'
            buf.extend(self.dlt_active_from)
            assert len(self.dlt_expires_at) == 8, f'dlt_expires_at must be 8 bytes, got {len(self.dlt_expires_at)}'
            buf.extend(self.dlt_expires_at)
            buf.extend(self.dlt_refresh_count.to_bytes(4, 'little'))
            buf.extend(self.dlt_max_refresh.to_bytes(4, 'little'))
            buf.extend(self.dlt_generation.to_bytes(8, 'little'))
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'KyberFullKEM':
            off = 0
            kyb_pubkey = bytes(data[off:off+1184]); off += 1184
            kyb_privkey = bytes(data[off:off+2400]); off += 2400
            kyb_sig_bytes = bytes(data[off:off+3309]); off += 3309
            dlt_auth_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_address = bytes(data[off:off+32]); off += 32
            dlt_auth_address = bytes(data[off:off+32]); off += 32
            dlt_created_at = bytes(data[off:off+8]); off += 8
            dlt_active_from = bytes(data[off:off+8]); off += 8
            dlt_expires_at = bytes(data[off:off+8]); off += 8
            dlt_refresh_count = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_max_refresh = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_generation = int.from_bytes(data[off:off+8], 'little'); off += 8
            return cls(kyb_pubkey=kyb_pubkey, kyb_privkey=kyb_privkey, kyb_sig_bytes=kyb_sig_bytes, dlt_auth_pubkey=dlt_auth_pubkey, dlt_root_pubkey=dlt_root_pubkey, dlt_root_address=dlt_root_address, dlt_auth_address=dlt_auth_address, dlt_created_at=dlt_created_at, dlt_active_from=dlt_active_from, dlt_expires_at=dlt_expires_at, dlt_refresh_count=dlt_refresh_count, dlt_max_refresh=dlt_max_refresh, dlt_generation=dlt_generation)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 10901

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.kyb_pubkey) == 1184, f'kyb_pubkey must be 1184 bytes, got {len(self.kyb_pubkey)}'
            buf.extend(self.kyb_pubkey)
            assert len(self.kyb_privkey) == 2400, f'kyb_privkey must be 2400 bytes, got {len(self.kyb_privkey)}'
            buf.extend(self.kyb_privkey)
            assert len(self.kyb_sig_bytes) == 3309, f'kyb_sig_bytes must be 3309 bytes, got {len(self.kyb_sig_bytes)}'
            buf.extend(self.kyb_sig_bytes)
            assert len(self.dlt_auth_pubkey) == 1952, f'dlt_auth_pubkey must be 1952 bytes, got {len(self.dlt_auth_pubkey)}'
            buf.extend(self.dlt_auth_pubkey)
            assert len(self.dlt_root_pubkey) == 1952, f'dlt_root_pubkey must be 1952 bytes, got {len(self.dlt_root_pubkey)}'
            buf.extend(self.dlt_root_pubkey)
            assert len(self.dlt_root_address) == 32, f'dlt_root_address must be 32 bytes, got {len(self.dlt_root_address)}'
            buf.extend(self.dlt_root_address)
            assert len(self.dlt_auth_address) == 32, f'dlt_auth_address must be 32 bytes, got {len(self.dlt_auth_address)}'
            buf.extend(self.dlt_auth_address)
            assert len(self.dlt_created_at) == 8, f'dlt_created_at must be 8 bytes, got {len(self.dlt_created_at)}'
            buf.extend(self.dlt_created_at)
            assert len(self.dlt_active_from) == 8, f'dlt_active_from must be 8 bytes, got {len(self.dlt_active_from)}'
            buf.extend(self.dlt_active_from)
            assert len(self.dlt_expires_at) == 8, f'dlt_expires_at must be 8 bytes, got {len(self.dlt_expires_at)}'
            buf.extend(self.dlt_expires_at)
            buf.extend(self.dlt_refresh_count.to_bytes(4, 'little'))
            buf.extend(self.dlt_max_refresh.to_bytes(4, 'little'))
            buf.extend(self.dlt_generation.to_bytes(8, 'little'))
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'KyberFullKEM':
            off = 0
            kyb_pubkey = bytes(data[off:off+1184]); off += 1184
            kyb_privkey = bytes(data[off:off+2400]); off += 2400
            kyb_sig_bytes = bytes(data[off:off+3309]); off += 3309
            dlt_auth_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_address = bytes(data[off:off+32]); off += 32
            dlt_auth_address = bytes(data[off:off+32]); off += 32
            dlt_created_at = bytes(data[off:off+8]); off += 8
            dlt_active_from = bytes(data[off:off+8]); off += 8
            dlt_expires_at = bytes(data[off:off+8]); off += 8
            dlt_refresh_count = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_max_refresh = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_generation = int.from_bytes(data[off:off+8], 'little'); off += 8
            return cls(kyb_pubkey=kyb_pubkey, kyb_privkey=kyb_privkey, kyb_sig_bytes=kyb_sig_bytes, dlt_auth_pubkey=dlt_auth_pubkey, dlt_root_pubkey=dlt_root_pubkey, dlt_root_address=dlt_root_address, dlt_auth_address=dlt_auth_address, dlt_created_at=dlt_created_at, dlt_active_from=dlt_active_from, dlt_expires_at=dlt_expires_at, dlt_refresh_count=dlt_refresh_count, dlt_max_refresh=dlt_max_refresh, dlt_generation=dlt_generation)

    class KyberPublicKEM(_PluginModel):
        kyb_pubkey: KyberPubkey
        kyb_sig_bytes: DilithiumSignature
        dlt_auth_pubkey: DilithiumPubkey
        dlt_root_pubkey: DilithiumPubkey
        dlt_root_address: AloecryptAddress
        dlt_auth_address: AloecryptAddress
        dlt_created_at: Timestamp
        dlt_active_from: Timestamp
        dlt_expires_at: Timestamp
        dlt_refresh_count: int
        dlt_max_refresh: int
        dlt_generation: int

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 8501

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(8501)
            off = 0
            assert len(self.kyb_pubkey) == 1184, f'kyb_pubkey must be 1184 bytes, got {len(self.kyb_pubkey)}'
            buf.extend(self.kyb_pubkey)
            assert len(self.kyb_sig_bytes) == 3309, f'kyb_sig_bytes must be 3309 bytes, got {len(self.kyb_sig_bytes)}'
            buf.extend(self.kyb_sig_bytes)
            assert len(self.dlt_auth_pubkey) == 1952, f'dlt_auth_pubkey must be 1952 bytes, got {len(self.dlt_auth_pubkey)}'
            buf.extend(self.dlt_auth_pubkey)
            assert len(self.dlt_root_pubkey) == 1952, f'dlt_root_pubkey must be 1952 bytes, got {len(self.dlt_root_pubkey)}'
            buf.extend(self.dlt_root_pubkey)
            assert len(self.dlt_root_address) == 32, f'dlt_root_address must be 32 bytes, got {len(self.dlt_root_address)}'
            buf.extend(self.dlt_root_address)
            assert len(self.dlt_auth_address) == 32, f'dlt_auth_address must be 32 bytes, got {len(self.dlt_auth_address)}'
            buf.extend(self.dlt_auth_address)
            assert len(self.dlt_created_at) == 8, f'dlt_created_at must be 8 bytes, got {len(self.dlt_created_at)}'
            buf.extend(self.dlt_created_at)
            assert len(self.dlt_active_from) == 8, f'dlt_active_from must be 8 bytes, got {len(self.dlt_active_from)}'
            buf.extend(self.dlt_active_from)
            assert len(self.dlt_expires_at) == 8, f'dlt_expires_at must be 8 bytes, got {len(self.dlt_expires_at)}'
            buf.extend(self.dlt_expires_at)
            buf.extend(self.dlt_refresh_count.to_bytes(4, 'little'))
            buf.extend(self.dlt_max_refresh.to_bytes(4, 'little'))
            buf.extend(self.dlt_generation.to_bytes(8, 'little'))
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'KyberPublicKEM':
            off = 0
            kyb_pubkey = bytes(data[off:off+1184]); off += 1184
            kyb_sig_bytes = bytes(data[off:off+3309]); off += 3309
            dlt_auth_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_address = bytes(data[off:off+32]); off += 32
            dlt_auth_address = bytes(data[off:off+32]); off += 32
            dlt_created_at = bytes(data[off:off+8]); off += 8
            dlt_active_from = bytes(data[off:off+8]); off += 8
            dlt_expires_at = bytes(data[off:off+8]); off += 8
            dlt_refresh_count = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_max_refresh = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_generation = int.from_bytes(data[off:off+8], 'little'); off += 8
            return cls(kyb_pubkey=kyb_pubkey, kyb_sig_bytes=kyb_sig_bytes, dlt_auth_pubkey=dlt_auth_pubkey, dlt_root_pubkey=dlt_root_pubkey, dlt_root_address=dlt_root_address, dlt_auth_address=dlt_auth_address, dlt_created_at=dlt_created_at, dlt_active_from=dlt_active_from, dlt_expires_at=dlt_expires_at, dlt_refresh_count=dlt_refresh_count, dlt_max_refresh=dlt_max_refresh, dlt_generation=dlt_generation)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 8501

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.kyb_pubkey) == 1184, f'kyb_pubkey must be 1184 bytes, got {len(self.kyb_pubkey)}'
            buf.extend(self.kyb_pubkey)
            assert len(self.kyb_sig_bytes) == 3309, f'kyb_sig_bytes must be 3309 bytes, got {len(self.kyb_sig_bytes)}'
            buf.extend(self.kyb_sig_bytes)
            assert len(self.dlt_auth_pubkey) == 1952, f'dlt_auth_pubkey must be 1952 bytes, got {len(self.dlt_auth_pubkey)}'
            buf.extend(self.dlt_auth_pubkey)
            assert len(self.dlt_root_pubkey) == 1952, f'dlt_root_pubkey must be 1952 bytes, got {len(self.dlt_root_pubkey)}'
            buf.extend(self.dlt_root_pubkey)
            assert len(self.dlt_root_address) == 32, f'dlt_root_address must be 32 bytes, got {len(self.dlt_root_address)}'
            buf.extend(self.dlt_root_address)
            assert len(self.dlt_auth_address) == 32, f'dlt_auth_address must be 32 bytes, got {len(self.dlt_auth_address)}'
            buf.extend(self.dlt_auth_address)
            assert len(self.dlt_created_at) == 8, f'dlt_created_at must be 8 bytes, got {len(self.dlt_created_at)}'
            buf.extend(self.dlt_created_at)
            assert len(self.dlt_active_from) == 8, f'dlt_active_from must be 8 bytes, got {len(self.dlt_active_from)}'
            buf.extend(self.dlt_active_from)
            assert len(self.dlt_expires_at) == 8, f'dlt_expires_at must be 8 bytes, got {len(self.dlt_expires_at)}'
            buf.extend(self.dlt_expires_at)
            buf.extend(self.dlt_refresh_count.to_bytes(4, 'little'))
            buf.extend(self.dlt_max_refresh.to_bytes(4, 'little'))
            buf.extend(self.dlt_generation.to_bytes(8, 'little'))
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'KyberPublicKEM':
            off = 0
            kyb_pubkey = bytes(data[off:off+1184]); off += 1184
            kyb_sig_bytes = bytes(data[off:off+3309]); off += 3309
            dlt_auth_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_address = bytes(data[off:off+32]); off += 32
            dlt_auth_address = bytes(data[off:off+32]); off += 32
            dlt_created_at = bytes(data[off:off+8]); off += 8
            dlt_active_from = bytes(data[off:off+8]); off += 8
            dlt_expires_at = bytes(data[off:off+8]); off += 8
            dlt_refresh_count = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_max_refresh = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_generation = int.from_bytes(data[off:off+8], 'little'); off += 8
            return cls(kyb_pubkey=kyb_pubkey, kyb_sig_bytes=kyb_sig_bytes, dlt_auth_pubkey=dlt_auth_pubkey, dlt_root_pubkey=dlt_root_pubkey, dlt_root_address=dlt_root_address, dlt_auth_address=dlt_auth_address, dlt_created_at=dlt_created_at, dlt_active_from=dlt_active_from, dlt_expires_at=dlt_expires_at, dlt_refresh_count=dlt_refresh_count, dlt_max_refresh=dlt_max_refresh, dlt_generation=dlt_generation)

    class XKyberFullKEM(_PluginModel):
        kyb_pubkey: KyberPubkey
        x_kyb_privkey: XKyberPrivkey
        kyb_sig_bytes: DilithiumSignature
        dlt_auth_pubkey: DilithiumPubkey
        dlt_root_pubkey: DilithiumPubkey
        dlt_root_address: AloecryptAddress
        dlt_auth_address: AloecryptAddress
        dlt_created_at: Timestamp
        dlt_active_from: Timestamp
        dlt_expires_at: Timestamp
        kyb_priv_hash: AloecryptHash
        dlt_refresh_count: int
        dlt_max_refresh: int
        dlt_generation: int
        un_hash: AloecryptHash
        nonce: ChachaNonce

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 10993

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(10993)
            off = 0
            assert len(self.kyb_pubkey) == 1184, f'kyb_pubkey must be 1184 bytes, got {len(self.kyb_pubkey)}'
            buf.extend(self.kyb_pubkey)
            assert len(self.x_kyb_privkey) == 2416, f'x_kyb_privkey must be 2416 bytes, got {len(self.x_kyb_privkey)}'
            buf.extend(self.x_kyb_privkey)
            assert len(self.kyb_sig_bytes) == 3309, f'kyb_sig_bytes must be 3309 bytes, got {len(self.kyb_sig_bytes)}'
            buf.extend(self.kyb_sig_bytes)
            assert len(self.dlt_auth_pubkey) == 1952, f'dlt_auth_pubkey must be 1952 bytes, got {len(self.dlt_auth_pubkey)}'
            buf.extend(self.dlt_auth_pubkey)
            assert len(self.dlt_root_pubkey) == 1952, f'dlt_root_pubkey must be 1952 bytes, got {len(self.dlt_root_pubkey)}'
            buf.extend(self.dlt_root_pubkey)
            assert len(self.dlt_root_address) == 32, f'dlt_root_address must be 32 bytes, got {len(self.dlt_root_address)}'
            buf.extend(self.dlt_root_address)
            assert len(self.dlt_auth_address) == 32, f'dlt_auth_address must be 32 bytes, got {len(self.dlt_auth_address)}'
            buf.extend(self.dlt_auth_address)
            assert len(self.dlt_created_at) == 8, f'dlt_created_at must be 8 bytes, got {len(self.dlt_created_at)}'
            buf.extend(self.dlt_created_at)
            assert len(self.dlt_active_from) == 8, f'dlt_active_from must be 8 bytes, got {len(self.dlt_active_from)}'
            buf.extend(self.dlt_active_from)
            assert len(self.dlt_expires_at) == 8, f'dlt_expires_at must be 8 bytes, got {len(self.dlt_expires_at)}'
            buf.extend(self.dlt_expires_at)
            assert len(self.kyb_priv_hash) == 32, f'kyb_priv_hash must be 32 bytes, got {len(self.kyb_priv_hash)}'
            buf.extend(self.kyb_priv_hash)
            buf.extend(self.dlt_refresh_count.to_bytes(4, 'little'))
            buf.extend(self.dlt_max_refresh.to_bytes(4, 'little'))
            buf.extend(self.dlt_generation.to_bytes(8, 'little'))
            assert len(self.un_hash) == 32, f'un_hash must be 32 bytes, got {len(self.un_hash)}'
            buf.extend(self.un_hash)
            assert len(self.nonce) == 12, f'nonce must be 12 bytes, got {len(self.nonce)}'
            buf.extend(self.nonce)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'XKyberFullKEM':
            off = 0
            kyb_pubkey = bytes(data[off:off+1184]); off += 1184
            x_kyb_privkey = bytes(data[off:off+2416]); off += 2416
            kyb_sig_bytes = bytes(data[off:off+3309]); off += 3309
            dlt_auth_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_address = bytes(data[off:off+32]); off += 32
            dlt_auth_address = bytes(data[off:off+32]); off += 32
            dlt_created_at = bytes(data[off:off+8]); off += 8
            dlt_active_from = bytes(data[off:off+8]); off += 8
            dlt_expires_at = bytes(data[off:off+8]); off += 8
            kyb_priv_hash = bytes(data[off:off+32]); off += 32
            dlt_refresh_count = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_max_refresh = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_generation = int.from_bytes(data[off:off+8], 'little'); off += 8
            un_hash = bytes(data[off:off+32]); off += 32
            nonce = bytes(data[off:off+12]); off += 12
            return cls(kyb_pubkey=kyb_pubkey, x_kyb_privkey=x_kyb_privkey, kyb_sig_bytes=kyb_sig_bytes, dlt_auth_pubkey=dlt_auth_pubkey, dlt_root_pubkey=dlt_root_pubkey, dlt_root_address=dlt_root_address, dlt_auth_address=dlt_auth_address, dlt_created_at=dlt_created_at, dlt_active_from=dlt_active_from, dlt_expires_at=dlt_expires_at, kyb_priv_hash=kyb_priv_hash, dlt_refresh_count=dlt_refresh_count, dlt_max_refresh=dlt_max_refresh, dlt_generation=dlt_generation, un_hash=un_hash, nonce=nonce)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 10993

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.kyb_pubkey) == 1184, f'kyb_pubkey must be 1184 bytes, got {len(self.kyb_pubkey)}'
            buf.extend(self.kyb_pubkey)
            assert len(self.x_kyb_privkey) == 2416, f'x_kyb_privkey must be 2416 bytes, got {len(self.x_kyb_privkey)}'
            buf.extend(self.x_kyb_privkey)
            assert len(self.kyb_sig_bytes) == 3309, f'kyb_sig_bytes must be 3309 bytes, got {len(self.kyb_sig_bytes)}'
            buf.extend(self.kyb_sig_bytes)
            assert len(self.dlt_auth_pubkey) == 1952, f'dlt_auth_pubkey must be 1952 bytes, got {len(self.dlt_auth_pubkey)}'
            buf.extend(self.dlt_auth_pubkey)
            assert len(self.dlt_root_pubkey) == 1952, f'dlt_root_pubkey must be 1952 bytes, got {len(self.dlt_root_pubkey)}'
            buf.extend(self.dlt_root_pubkey)
            assert len(self.dlt_root_address) == 32, f'dlt_root_address must be 32 bytes, got {len(self.dlt_root_address)}'
            buf.extend(self.dlt_root_address)
            assert len(self.dlt_auth_address) == 32, f'dlt_auth_address must be 32 bytes, got {len(self.dlt_auth_address)}'
            buf.extend(self.dlt_auth_address)
            assert len(self.dlt_created_at) == 8, f'dlt_created_at must be 8 bytes, got {len(self.dlt_created_at)}'
            buf.extend(self.dlt_created_at)
            assert len(self.dlt_active_from) == 8, f'dlt_active_from must be 8 bytes, got {len(self.dlt_active_from)}'
            buf.extend(self.dlt_active_from)
            assert len(self.dlt_expires_at) == 8, f'dlt_expires_at must be 8 bytes, got {len(self.dlt_expires_at)}'
            buf.extend(self.dlt_expires_at)
            assert len(self.kyb_priv_hash) == 32, f'kyb_priv_hash must be 32 bytes, got {len(self.kyb_priv_hash)}'
            buf.extend(self.kyb_priv_hash)
            buf.extend(self.dlt_refresh_count.to_bytes(4, 'little'))
            buf.extend(self.dlt_max_refresh.to_bytes(4, 'little'))
            buf.extend(self.dlt_generation.to_bytes(8, 'little'))
            assert len(self.un_hash) == 32, f'un_hash must be 32 bytes, got {len(self.un_hash)}'
            buf.extend(self.un_hash)
            assert len(self.nonce) == 12, f'nonce must be 12 bytes, got {len(self.nonce)}'
            buf.extend(self.nonce)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'XKyberFullKEM':
            off = 0
            kyb_pubkey = bytes(data[off:off+1184]); off += 1184
            x_kyb_privkey = bytes(data[off:off+2416]); off += 2416
            kyb_sig_bytes = bytes(data[off:off+3309]); off += 3309
            dlt_auth_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_address = bytes(data[off:off+32]); off += 32
            dlt_auth_address = bytes(data[off:off+32]); off += 32
            dlt_created_at = bytes(data[off:off+8]); off += 8
            dlt_active_from = bytes(data[off:off+8]); off += 8
            dlt_expires_at = bytes(data[off:off+8]); off += 8
            kyb_priv_hash = bytes(data[off:off+32]); off += 32
            dlt_refresh_count = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_max_refresh = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_generation = int.from_bytes(data[off:off+8], 'little'); off += 8
            un_hash = bytes(data[off:off+32]); off += 32
            nonce = bytes(data[off:off+12]); off += 12
            return cls(kyb_pubkey=kyb_pubkey, x_kyb_privkey=x_kyb_privkey, kyb_sig_bytes=kyb_sig_bytes, dlt_auth_pubkey=dlt_auth_pubkey, dlt_root_pubkey=dlt_root_pubkey, dlt_root_address=dlt_root_address, dlt_auth_address=dlt_auth_address, dlt_created_at=dlt_created_at, dlt_active_from=dlt_active_from, dlt_expires_at=dlt_expires_at, kyb_priv_hash=kyb_priv_hash, dlt_refresh_count=dlt_refresh_count, dlt_max_refresh=dlt_max_refresh, dlt_generation=dlt_generation, un_hash=un_hash, nonce=nonce)


KyberFullKEM = kem_api.KyberFullKEM
KyberPublicKEM = kem_api.KyberPublicKEM
XKyberFullKEM = kem_api.XKyberFullKEM

class signatory_api:
    class DilithiumSigner(_PluginModel):
        dlt_pubkey: DilithiumPubkey
        dlt_privkey: DilithiumPrivkey
        dlt_sig_bytes: DilithiumSignature
        dlt_auth_pubkey: DilithiumPubkey
        dlt_root_pubkey: DilithiumPubkey
        dlt_privseed: DilithiumPrivSeed
        dlt_auth_address: AloecryptAddress
        dlt_root_address: AloecryptAddress
        dlt_generation: int
        dlt_created_at: Timestamp
        dlt_active_from: Timestamp
        dlt_expires_at: Timestamp
        dlt_refresh_count: int
        dlt_max_refresh: int

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 13333

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(13333)
            off = 0
            assert len(self.dlt_pubkey) == 1952, f'dlt_pubkey must be 1952 bytes, got {len(self.dlt_pubkey)}'
            buf.extend(self.dlt_pubkey)
            assert len(self.dlt_privkey) == 4032, f'dlt_privkey must be 4032 bytes, got {len(self.dlt_privkey)}'
            buf.extend(self.dlt_privkey)
            assert len(self.dlt_sig_bytes) == 3309, f'dlt_sig_bytes must be 3309 bytes, got {len(self.dlt_sig_bytes)}'
            buf.extend(self.dlt_sig_bytes)
            assert len(self.dlt_auth_pubkey) == 1952, f'dlt_auth_pubkey must be 1952 bytes, got {len(self.dlt_auth_pubkey)}'
            buf.extend(self.dlt_auth_pubkey)
            assert len(self.dlt_root_pubkey) == 1952, f'dlt_root_pubkey must be 1952 bytes, got {len(self.dlt_root_pubkey)}'
            buf.extend(self.dlt_root_pubkey)
            assert len(self.dlt_privseed) == 32, f'dlt_privseed must be 32 bytes, got {len(self.dlt_privseed)}'
            buf.extend(self.dlt_privseed)
            assert len(self.dlt_auth_address) == 32, f'dlt_auth_address must be 32 bytes, got {len(self.dlt_auth_address)}'
            buf.extend(self.dlt_auth_address)
            assert len(self.dlt_root_address) == 32, f'dlt_root_address must be 32 bytes, got {len(self.dlt_root_address)}'
            buf.extend(self.dlt_root_address)
            buf.extend(self.dlt_generation.to_bytes(8, 'little'))
            assert len(self.dlt_created_at) == 8, f'dlt_created_at must be 8 bytes, got {len(self.dlt_created_at)}'
            buf.extend(self.dlt_created_at)
            assert len(self.dlt_active_from) == 8, f'dlt_active_from must be 8 bytes, got {len(self.dlt_active_from)}'
            buf.extend(self.dlt_active_from)
            assert len(self.dlt_expires_at) == 8, f'dlt_expires_at must be 8 bytes, got {len(self.dlt_expires_at)}'
            buf.extend(self.dlt_expires_at)
            buf.extend(self.dlt_refresh_count.to_bytes(4, 'little'))
            buf.extend(self.dlt_max_refresh.to_bytes(4, 'little'))
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'DilithiumSigner':
            off = 0
            dlt_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_privkey = bytes(data[off:off+4032]); off += 4032
            dlt_sig_bytes = bytes(data[off:off+3309]); off += 3309
            dlt_auth_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_privseed = bytes(data[off:off+32]); off += 32
            dlt_auth_address = bytes(data[off:off+32]); off += 32
            dlt_root_address = bytes(data[off:off+32]); off += 32
            dlt_generation = int.from_bytes(data[off:off+8], 'little'); off += 8
            dlt_created_at = bytes(data[off:off+8]); off += 8
            dlt_active_from = bytes(data[off:off+8]); off += 8
            dlt_expires_at = bytes(data[off:off+8]); off += 8
            dlt_refresh_count = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_max_refresh = int.from_bytes(data[off:off+4], 'little'); off += 4
            return cls(dlt_pubkey=dlt_pubkey, dlt_privkey=dlt_privkey, dlt_sig_bytes=dlt_sig_bytes, dlt_auth_pubkey=dlt_auth_pubkey, dlt_root_pubkey=dlt_root_pubkey, dlt_privseed=dlt_privseed, dlt_auth_address=dlt_auth_address, dlt_root_address=dlt_root_address, dlt_generation=dlt_generation, dlt_created_at=dlt_created_at, dlt_active_from=dlt_active_from, dlt_expires_at=dlt_expires_at, dlt_refresh_count=dlt_refresh_count, dlt_max_refresh=dlt_max_refresh)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 13333

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.dlt_pubkey) == 1952, f'dlt_pubkey must be 1952 bytes, got {len(self.dlt_pubkey)}'
            buf.extend(self.dlt_pubkey)
            assert len(self.dlt_privkey) == 4032, f'dlt_privkey must be 4032 bytes, got {len(self.dlt_privkey)}'
            buf.extend(self.dlt_privkey)
            assert len(self.dlt_sig_bytes) == 3309, f'dlt_sig_bytes must be 3309 bytes, got {len(self.dlt_sig_bytes)}'
            buf.extend(self.dlt_sig_bytes)
            assert len(self.dlt_auth_pubkey) == 1952, f'dlt_auth_pubkey must be 1952 bytes, got {len(self.dlt_auth_pubkey)}'
            buf.extend(self.dlt_auth_pubkey)
            assert len(self.dlt_root_pubkey) == 1952, f'dlt_root_pubkey must be 1952 bytes, got {len(self.dlt_root_pubkey)}'
            buf.extend(self.dlt_root_pubkey)
            assert len(self.dlt_privseed) == 32, f'dlt_privseed must be 32 bytes, got {len(self.dlt_privseed)}'
            buf.extend(self.dlt_privseed)
            assert len(self.dlt_auth_address) == 32, f'dlt_auth_address must be 32 bytes, got {len(self.dlt_auth_address)}'
            buf.extend(self.dlt_auth_address)
            assert len(self.dlt_root_address) == 32, f'dlt_root_address must be 32 bytes, got {len(self.dlt_root_address)}'
            buf.extend(self.dlt_root_address)
            buf.extend(self.dlt_generation.to_bytes(8, 'little'))
            assert len(self.dlt_created_at) == 8, f'dlt_created_at must be 8 bytes, got {len(self.dlt_created_at)}'
            buf.extend(self.dlt_created_at)
            assert len(self.dlt_active_from) == 8, f'dlt_active_from must be 8 bytes, got {len(self.dlt_active_from)}'
            buf.extend(self.dlt_active_from)
            assert len(self.dlt_expires_at) == 8, f'dlt_expires_at must be 8 bytes, got {len(self.dlt_expires_at)}'
            buf.extend(self.dlt_expires_at)
            buf.extend(self.dlt_refresh_count.to_bytes(4, 'little'))
            buf.extend(self.dlt_max_refresh.to_bytes(4, 'little'))
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'DilithiumSigner':
            off = 0
            dlt_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_privkey = bytes(data[off:off+4032]); off += 4032
            dlt_sig_bytes = bytes(data[off:off+3309]); off += 3309
            dlt_auth_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_privseed = bytes(data[off:off+32]); off += 32
            dlt_auth_address = bytes(data[off:off+32]); off += 32
            dlt_root_address = bytes(data[off:off+32]); off += 32
            dlt_generation = int.from_bytes(data[off:off+8], 'little'); off += 8
            dlt_created_at = bytes(data[off:off+8]); off += 8
            dlt_active_from = bytes(data[off:off+8]); off += 8
            dlt_expires_at = bytes(data[off:off+8]); off += 8
            dlt_refresh_count = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_max_refresh = int.from_bytes(data[off:off+4], 'little'); off += 4
            return cls(dlt_pubkey=dlt_pubkey, dlt_privkey=dlt_privkey, dlt_sig_bytes=dlt_sig_bytes, dlt_auth_pubkey=dlt_auth_pubkey, dlt_root_pubkey=dlt_root_pubkey, dlt_privseed=dlt_privseed, dlt_auth_address=dlt_auth_address, dlt_root_address=dlt_root_address, dlt_generation=dlt_generation, dlt_created_at=dlt_created_at, dlt_active_from=dlt_active_from, dlt_expires_at=dlt_expires_at, dlt_refresh_count=dlt_refresh_count, dlt_max_refresh=dlt_max_refresh)

    class DilithiumVerifier(_PluginModel):
        dlt_pubkey: DilithiumPubkey
        dlt_sig_bytes: DilithiumSignature
        dlt_auth_pubkey: DilithiumPubkey
        dlt_root_pubkey: DilithiumPubkey
        dlt_root_address: AloecryptAddress
        dlt_auth_address: AloecryptAddress
        dlt_generation: int
        dlt_created_at: Timestamp
        dlt_active_from: Timestamp
        dlt_expires_at: Timestamp
        dlt_refresh_count: int
        dlt_max_refresh: int

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 9269

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(9269)
            off = 0
            assert len(self.dlt_pubkey) == 1952, f'dlt_pubkey must be 1952 bytes, got {len(self.dlt_pubkey)}'
            buf.extend(self.dlt_pubkey)
            assert len(self.dlt_sig_bytes) == 3309, f'dlt_sig_bytes must be 3309 bytes, got {len(self.dlt_sig_bytes)}'
            buf.extend(self.dlt_sig_bytes)
            assert len(self.dlt_auth_pubkey) == 1952, f'dlt_auth_pubkey must be 1952 bytes, got {len(self.dlt_auth_pubkey)}'
            buf.extend(self.dlt_auth_pubkey)
            assert len(self.dlt_root_pubkey) == 1952, f'dlt_root_pubkey must be 1952 bytes, got {len(self.dlt_root_pubkey)}'
            buf.extend(self.dlt_root_pubkey)
            assert len(self.dlt_root_address) == 32, f'dlt_root_address must be 32 bytes, got {len(self.dlt_root_address)}'
            buf.extend(self.dlt_root_address)
            assert len(self.dlt_auth_address) == 32, f'dlt_auth_address must be 32 bytes, got {len(self.dlt_auth_address)}'
            buf.extend(self.dlt_auth_address)
            buf.extend(self.dlt_generation.to_bytes(8, 'little'))
            assert len(self.dlt_created_at) == 8, f'dlt_created_at must be 8 bytes, got {len(self.dlt_created_at)}'
            buf.extend(self.dlt_created_at)
            assert len(self.dlt_active_from) == 8, f'dlt_active_from must be 8 bytes, got {len(self.dlt_active_from)}'
            buf.extend(self.dlt_active_from)
            assert len(self.dlt_expires_at) == 8, f'dlt_expires_at must be 8 bytes, got {len(self.dlt_expires_at)}'
            buf.extend(self.dlt_expires_at)
            buf.extend(self.dlt_refresh_count.to_bytes(4, 'little'))
            buf.extend(self.dlt_max_refresh.to_bytes(4, 'little'))
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'DilithiumVerifier':
            off = 0
            dlt_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_sig_bytes = bytes(data[off:off+3309]); off += 3309
            dlt_auth_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_address = bytes(data[off:off+32]); off += 32
            dlt_auth_address = bytes(data[off:off+32]); off += 32
            dlt_generation = int.from_bytes(data[off:off+8], 'little'); off += 8
            dlt_created_at = bytes(data[off:off+8]); off += 8
            dlt_active_from = bytes(data[off:off+8]); off += 8
            dlt_expires_at = bytes(data[off:off+8]); off += 8
            dlt_refresh_count = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_max_refresh = int.from_bytes(data[off:off+4], 'little'); off += 4
            return cls(dlt_pubkey=dlt_pubkey, dlt_sig_bytes=dlt_sig_bytes, dlt_auth_pubkey=dlt_auth_pubkey, dlt_root_pubkey=dlt_root_pubkey, dlt_root_address=dlt_root_address, dlt_auth_address=dlt_auth_address, dlt_generation=dlt_generation, dlt_created_at=dlt_created_at, dlt_active_from=dlt_active_from, dlt_expires_at=dlt_expires_at, dlt_refresh_count=dlt_refresh_count, dlt_max_refresh=dlt_max_refresh)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 9269

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.dlt_pubkey) == 1952, f'dlt_pubkey must be 1952 bytes, got {len(self.dlt_pubkey)}'
            buf.extend(self.dlt_pubkey)
            assert len(self.dlt_sig_bytes) == 3309, f'dlt_sig_bytes must be 3309 bytes, got {len(self.dlt_sig_bytes)}'
            buf.extend(self.dlt_sig_bytes)
            assert len(self.dlt_auth_pubkey) == 1952, f'dlt_auth_pubkey must be 1952 bytes, got {len(self.dlt_auth_pubkey)}'
            buf.extend(self.dlt_auth_pubkey)
            assert len(self.dlt_root_pubkey) == 1952, f'dlt_root_pubkey must be 1952 bytes, got {len(self.dlt_root_pubkey)}'
            buf.extend(self.dlt_root_pubkey)
            assert len(self.dlt_root_address) == 32, f'dlt_root_address must be 32 bytes, got {len(self.dlt_root_address)}'
            buf.extend(self.dlt_root_address)
            assert len(self.dlt_auth_address) == 32, f'dlt_auth_address must be 32 bytes, got {len(self.dlt_auth_address)}'
            buf.extend(self.dlt_auth_address)
            buf.extend(self.dlt_generation.to_bytes(8, 'little'))
            assert len(self.dlt_created_at) == 8, f'dlt_created_at must be 8 bytes, got {len(self.dlt_created_at)}'
            buf.extend(self.dlt_created_at)
            assert len(self.dlt_active_from) == 8, f'dlt_active_from must be 8 bytes, got {len(self.dlt_active_from)}'
            buf.extend(self.dlt_active_from)
            assert len(self.dlt_expires_at) == 8, f'dlt_expires_at must be 8 bytes, got {len(self.dlt_expires_at)}'
            buf.extend(self.dlt_expires_at)
            buf.extend(self.dlt_refresh_count.to_bytes(4, 'little'))
            buf.extend(self.dlt_max_refresh.to_bytes(4, 'little'))
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'DilithiumVerifier':
            off = 0
            dlt_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_sig_bytes = bytes(data[off:off+3309]); off += 3309
            dlt_auth_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_address = bytes(data[off:off+32]); off += 32
            dlt_auth_address = bytes(data[off:off+32]); off += 32
            dlt_generation = int.from_bytes(data[off:off+8], 'little'); off += 8
            dlt_created_at = bytes(data[off:off+8]); off += 8
            dlt_active_from = bytes(data[off:off+8]); off += 8
            dlt_expires_at = bytes(data[off:off+8]); off += 8
            dlt_refresh_count = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_max_refresh = int.from_bytes(data[off:off+4], 'little'); off += 4
            return cls(dlt_pubkey=dlt_pubkey, dlt_sig_bytes=dlt_sig_bytes, dlt_auth_pubkey=dlt_auth_pubkey, dlt_root_pubkey=dlt_root_pubkey, dlt_root_address=dlt_root_address, dlt_auth_address=dlt_auth_address, dlt_generation=dlt_generation, dlt_created_at=dlt_created_at, dlt_active_from=dlt_active_from, dlt_expires_at=dlt_expires_at, dlt_refresh_count=dlt_refresh_count, dlt_max_refresh=dlt_max_refresh)

    class XDilithiumSigner(_PluginModel):
        dlt_pubkey: DilithiumPubkey
        dlt_sig_bytes: DilithiumSignature
        dlt_auth_pubkey: DilithiumPubkey
        dlt_root_pubkey: DilithiumPubkey
        x_dlt_privseed: XDilithiumPrivSeed
        dlt_auth_address: AloecryptAddress
        dlt_root_address: AloecryptAddress
        dlt_created_at: Timestamp
        dlt_active_from: Timestamp
        dlt_expires_at: Timestamp
        dlt_refresh_count: int
        dlt_max_refresh: int
        dlt_generation: int
        dlt_priv_hash: AloecryptHash
        un_hash: AloecryptHash
        nonce: ChachaNonce

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 9393

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(9393)
            off = 0
            assert len(self.dlt_pubkey) == 1952, f'dlt_pubkey must be 1952 bytes, got {len(self.dlt_pubkey)}'
            buf.extend(self.dlt_pubkey)
            assert len(self.dlt_sig_bytes) == 3309, f'dlt_sig_bytes must be 3309 bytes, got {len(self.dlt_sig_bytes)}'
            buf.extend(self.dlt_sig_bytes)
            assert len(self.dlt_auth_pubkey) == 1952, f'dlt_auth_pubkey must be 1952 bytes, got {len(self.dlt_auth_pubkey)}'
            buf.extend(self.dlt_auth_pubkey)
            assert len(self.dlt_root_pubkey) == 1952, f'dlt_root_pubkey must be 1952 bytes, got {len(self.dlt_root_pubkey)}'
            buf.extend(self.dlt_root_pubkey)
            assert len(self.x_dlt_privseed) == 48, f'x_dlt_privseed must be 48 bytes, got {len(self.x_dlt_privseed)}'
            buf.extend(self.x_dlt_privseed)
            assert len(self.dlt_auth_address) == 32, f'dlt_auth_address must be 32 bytes, got {len(self.dlt_auth_address)}'
            buf.extend(self.dlt_auth_address)
            assert len(self.dlt_root_address) == 32, f'dlt_root_address must be 32 bytes, got {len(self.dlt_root_address)}'
            buf.extend(self.dlt_root_address)
            assert len(self.dlt_created_at) == 8, f'dlt_created_at must be 8 bytes, got {len(self.dlt_created_at)}'
            buf.extend(self.dlt_created_at)
            assert len(self.dlt_active_from) == 8, f'dlt_active_from must be 8 bytes, got {len(self.dlt_active_from)}'
            buf.extend(self.dlt_active_from)
            assert len(self.dlt_expires_at) == 8, f'dlt_expires_at must be 8 bytes, got {len(self.dlt_expires_at)}'
            buf.extend(self.dlt_expires_at)
            buf.extend(self.dlt_refresh_count.to_bytes(4, 'little'))
            buf.extend(self.dlt_max_refresh.to_bytes(4, 'little'))
            buf.extend(self.dlt_generation.to_bytes(8, 'little'))
            assert len(self.dlt_priv_hash) == 32, f'dlt_priv_hash must be 32 bytes, got {len(self.dlt_priv_hash)}'
            buf.extend(self.dlt_priv_hash)
            assert len(self.un_hash) == 32, f'un_hash must be 32 bytes, got {len(self.un_hash)}'
            buf.extend(self.un_hash)
            assert len(self.nonce) == 12, f'nonce must be 12 bytes, got {len(self.nonce)}'
            buf.extend(self.nonce)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'XDilithiumSigner':
            off = 0
            dlt_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_sig_bytes = bytes(data[off:off+3309]); off += 3309
            dlt_auth_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_pubkey = bytes(data[off:off+1952]); off += 1952
            x_dlt_privseed = bytes(data[off:off+48]); off += 48
            dlt_auth_address = bytes(data[off:off+32]); off += 32
            dlt_root_address = bytes(data[off:off+32]); off += 32
            dlt_created_at = bytes(data[off:off+8]); off += 8
            dlt_active_from = bytes(data[off:off+8]); off += 8
            dlt_expires_at = bytes(data[off:off+8]); off += 8
            dlt_refresh_count = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_max_refresh = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_generation = int.from_bytes(data[off:off+8], 'little'); off += 8
            dlt_priv_hash = bytes(data[off:off+32]); off += 32
            un_hash = bytes(data[off:off+32]); off += 32
            nonce = bytes(data[off:off+12]); off += 12
            return cls(dlt_pubkey=dlt_pubkey, dlt_sig_bytes=dlt_sig_bytes, dlt_auth_pubkey=dlt_auth_pubkey, dlt_root_pubkey=dlt_root_pubkey, x_dlt_privseed=x_dlt_privseed, dlt_auth_address=dlt_auth_address, dlt_root_address=dlt_root_address, dlt_created_at=dlt_created_at, dlt_active_from=dlt_active_from, dlt_expires_at=dlt_expires_at, dlt_refresh_count=dlt_refresh_count, dlt_max_refresh=dlt_max_refresh, dlt_generation=dlt_generation, dlt_priv_hash=dlt_priv_hash, un_hash=un_hash, nonce=nonce)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 9393

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.dlt_pubkey) == 1952, f'dlt_pubkey must be 1952 bytes, got {len(self.dlt_pubkey)}'
            buf.extend(self.dlt_pubkey)
            assert len(self.dlt_sig_bytes) == 3309, f'dlt_sig_bytes must be 3309 bytes, got {len(self.dlt_sig_bytes)}'
            buf.extend(self.dlt_sig_bytes)
            assert len(self.dlt_auth_pubkey) == 1952, f'dlt_auth_pubkey must be 1952 bytes, got {len(self.dlt_auth_pubkey)}'
            buf.extend(self.dlt_auth_pubkey)
            assert len(self.dlt_root_pubkey) == 1952, f'dlt_root_pubkey must be 1952 bytes, got {len(self.dlt_root_pubkey)}'
            buf.extend(self.dlt_root_pubkey)
            assert len(self.x_dlt_privseed) == 48, f'x_dlt_privseed must be 48 bytes, got {len(self.x_dlt_privseed)}'
            buf.extend(self.x_dlt_privseed)
            assert len(self.dlt_auth_address) == 32, f'dlt_auth_address must be 32 bytes, got {len(self.dlt_auth_address)}'
            buf.extend(self.dlt_auth_address)
            assert len(self.dlt_root_address) == 32, f'dlt_root_address must be 32 bytes, got {len(self.dlt_root_address)}'
            buf.extend(self.dlt_root_address)
            assert len(self.dlt_created_at) == 8, f'dlt_created_at must be 8 bytes, got {len(self.dlt_created_at)}'
            buf.extend(self.dlt_created_at)
            assert len(self.dlt_active_from) == 8, f'dlt_active_from must be 8 bytes, got {len(self.dlt_active_from)}'
            buf.extend(self.dlt_active_from)
            assert len(self.dlt_expires_at) == 8, f'dlt_expires_at must be 8 bytes, got {len(self.dlt_expires_at)}'
            buf.extend(self.dlt_expires_at)
            buf.extend(self.dlt_refresh_count.to_bytes(4, 'little'))
            buf.extend(self.dlt_max_refresh.to_bytes(4, 'little'))
            buf.extend(self.dlt_generation.to_bytes(8, 'little'))
            assert len(self.dlt_priv_hash) == 32, f'dlt_priv_hash must be 32 bytes, got {len(self.dlt_priv_hash)}'
            buf.extend(self.dlt_priv_hash)
            assert len(self.un_hash) == 32, f'un_hash must be 32 bytes, got {len(self.un_hash)}'
            buf.extend(self.un_hash)
            assert len(self.nonce) == 12, f'nonce must be 12 bytes, got {len(self.nonce)}'
            buf.extend(self.nonce)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'XDilithiumSigner':
            off = 0
            dlt_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_sig_bytes = bytes(data[off:off+3309]); off += 3309
            dlt_auth_pubkey = bytes(data[off:off+1952]); off += 1952
            dlt_root_pubkey = bytes(data[off:off+1952]); off += 1952
            x_dlt_privseed = bytes(data[off:off+48]); off += 48
            dlt_auth_address = bytes(data[off:off+32]); off += 32
            dlt_root_address = bytes(data[off:off+32]); off += 32
            dlt_created_at = bytes(data[off:off+8]); off += 8
            dlt_active_from = bytes(data[off:off+8]); off += 8
            dlt_expires_at = bytes(data[off:off+8]); off += 8
            dlt_refresh_count = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_max_refresh = int.from_bytes(data[off:off+4], 'little'); off += 4
            dlt_generation = int.from_bytes(data[off:off+8], 'little'); off += 8
            dlt_priv_hash = bytes(data[off:off+32]); off += 32
            un_hash = bytes(data[off:off+32]); off += 32
            nonce = bytes(data[off:off+12]); off += 12
            return cls(dlt_pubkey=dlt_pubkey, dlt_sig_bytes=dlt_sig_bytes, dlt_auth_pubkey=dlt_auth_pubkey, dlt_root_pubkey=dlt_root_pubkey, x_dlt_privseed=x_dlt_privseed, dlt_auth_address=dlt_auth_address, dlt_root_address=dlt_root_address, dlt_created_at=dlt_created_at, dlt_active_from=dlt_active_from, dlt_expires_at=dlt_expires_at, dlt_refresh_count=dlt_refresh_count, dlt_max_refresh=dlt_max_refresh, dlt_generation=dlt_generation, dlt_priv_hash=dlt_priv_hash, un_hash=un_hash, nonce=nonce)


DilithiumSigner = signatory_api.DilithiumSigner
DilithiumVerifier = signatory_api.DilithiumVerifier
XDilithiumSigner = signatory_api.XDilithiumSigner

class party_api:
    class Party(_PluginModel):
        nonce: AloecryptSessionNonce
        session_signature: DilithiumSignature
        delegate_signer: DilithiumSigner
        stable_kem: KyberFullKEM
        session_kem: KyberFullKEM
        stable_secret: AloecryptSecret
        session_secret: AloecryptSecret

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 38540

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(38540)
            off = 0
            assert len(self.nonce) == 32, f'nonce must be 32 bytes, got {len(self.nonce)}'
            buf.extend(self.nonce)
            assert len(self.session_signature) == 3309, f'session_signature must be 3309 bytes, got {len(self.session_signature)}'
            buf.extend(self.session_signature)
            buf.extend(self.delegate_signer.to_wire_bytes())
            buf.extend(self.stable_kem.to_wire_bytes())
            buf.extend(self.session_kem.to_wire_bytes())
            assert len(self.stable_secret) == 32, f'stable_secret must be 32 bytes, got {len(self.stable_secret)}'
            buf.extend(self.stable_secret)
            assert len(self.session_secret) == 32, f'session_secret must be 32 bytes, got {len(self.session_secret)}'
            buf.extend(self.session_secret)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'Party':
            off = 0
            nonce = bytes(data[off:off+32]); off += 32
            session_signature = bytes(data[off:off+3309]); off += 3309
            delegate_signer = DilithiumSigner.from_wire_bytes(data[off:off+13333]); off += 13333
            stable_kem = KyberFullKEM.from_wire_bytes(data[off:off+10901]); off += 10901
            session_kem = KyberFullKEM.from_wire_bytes(data[off:off+10901]); off += 10901
            stable_secret = bytes(data[off:off+32]); off += 32
            session_secret = bytes(data[off:off+32]); off += 32
            return cls(nonce=nonce, session_signature=session_signature, delegate_signer=delegate_signer, stable_kem=stable_kem, session_kem=session_kem, stable_secret=stable_secret, session_secret=session_secret)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 38540

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.nonce) == 32, f'nonce must be 32 bytes, got {len(self.nonce)}'
            buf.extend(self.nonce)
            assert len(self.session_signature) == 3309, f'session_signature must be 3309 bytes, got {len(self.session_signature)}'
            buf.extend(self.session_signature)
            buf.extend(self.delegate_signer.to_wire_bytes())
            buf.extend(self.stable_kem.to_wire_bytes())
            buf.extend(self.session_kem.to_wire_bytes())
            assert len(self.stable_secret) == 32, f'stable_secret must be 32 bytes, got {len(self.stable_secret)}'
            buf.extend(self.stable_secret)
            assert len(self.session_secret) == 32, f'session_secret must be 32 bytes, got {len(self.session_secret)}'
            buf.extend(self.session_secret)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'Party':
            off = 0
            nonce = bytes(data[off:off+32]); off += 32
            session_signature = bytes(data[off:off+3309]); off += 3309
            delegate_signer = DilithiumSigner.from_wire_bytes(data[off:off+13333]); off += 13333
            stable_kem = KyberFullKEM.from_wire_bytes(data[off:off+10901]); off += 10901
            session_kem = KyberFullKEM.from_wire_bytes(data[off:off+10901]); off += 10901
            stable_secret = bytes(data[off:off+32]); off += 32
            session_secret = bytes(data[off:off+32]); off += 32
            return cls(nonce=nonce, session_signature=session_signature, delegate_signer=delegate_signer, stable_kem=stable_kem, session_kem=session_kem, stable_secret=stable_secret, session_secret=session_secret)

    class CounterParty(_PluginModel):
        address: AloecryptAddress
        nonce: AloecryptSessionNonce
        signature: DilithiumSignature
        stable_kem: KyberPublicKEM
        session_kem: KyberPublicKEM
        verifier: DilithiumVerifier
        stable_secret: AloecryptSecret
        session_secret: AloecryptSecret

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 29708

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(29708)
            off = 0
            assert len(self.address) == 32, f'address must be 32 bytes, got {len(self.address)}'
            buf.extend(self.address)
            assert len(self.nonce) == 32, f'nonce must be 32 bytes, got {len(self.nonce)}'
            buf.extend(self.nonce)
            assert len(self.signature) == 3309, f'signature must be 3309 bytes, got {len(self.signature)}'
            buf.extend(self.signature)
            buf.extend(self.stable_kem.to_wire_bytes())
            buf.extend(self.session_kem.to_wire_bytes())
            buf.extend(self.verifier.to_wire_bytes())
            assert len(self.stable_secret) == 32, f'stable_secret must be 32 bytes, got {len(self.stable_secret)}'
            buf.extend(self.stable_secret)
            assert len(self.session_secret) == 32, f'session_secret must be 32 bytes, got {len(self.session_secret)}'
            buf.extend(self.session_secret)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'CounterParty':
            off = 0
            address = bytes(data[off:off+32]); off += 32
            nonce = bytes(data[off:off+32]); off += 32
            signature = bytes(data[off:off+3309]); off += 3309
            stable_kem = KyberPublicKEM.from_wire_bytes(data[off:off+8501]); off += 8501
            session_kem = KyberPublicKEM.from_wire_bytes(data[off:off+8501]); off += 8501
            verifier = DilithiumVerifier.from_wire_bytes(data[off:off+9269]); off += 9269
            stable_secret = bytes(data[off:off+32]); off += 32
            session_secret = bytes(data[off:off+32]); off += 32
            return cls(address=address, nonce=nonce, signature=signature, stable_kem=stable_kem, session_kem=session_kem, verifier=verifier, stable_secret=stable_secret, session_secret=session_secret)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 29708

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.address) == 32, f'address must be 32 bytes, got {len(self.address)}'
            buf.extend(self.address)
            assert len(self.nonce) == 32, f'nonce must be 32 bytes, got {len(self.nonce)}'
            buf.extend(self.nonce)
            assert len(self.signature) == 3309, f'signature must be 3309 bytes, got {len(self.signature)}'
            buf.extend(self.signature)
            buf.extend(self.stable_kem.to_wire_bytes())
            buf.extend(self.session_kem.to_wire_bytes())
            buf.extend(self.verifier.to_wire_bytes())
            assert len(self.stable_secret) == 32, f'stable_secret must be 32 bytes, got {len(self.stable_secret)}'
            buf.extend(self.stable_secret)
            assert len(self.session_secret) == 32, f'session_secret must be 32 bytes, got {len(self.session_secret)}'
            buf.extend(self.session_secret)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'CounterParty':
            off = 0
            address = bytes(data[off:off+32]); off += 32
            nonce = bytes(data[off:off+32]); off += 32
            signature = bytes(data[off:off+3309]); off += 3309
            stable_kem = KyberPublicKEM.from_wire_bytes(data[off:off+8501]); off += 8501
            session_kem = KyberPublicKEM.from_wire_bytes(data[off:off+8501]); off += 8501
            verifier = DilithiumVerifier.from_wire_bytes(data[off:off+9269]); off += 9269
            stable_secret = bytes(data[off:off+32]); off += 32
            session_secret = bytes(data[off:off+32]); off += 32
            return cls(address=address, nonce=nonce, signature=signature, stable_kem=stable_kem, session_kem=session_kem, verifier=verifier, stable_secret=stable_secret, session_secret=session_secret)

    class XParty(_PluginModel):
        nonce: AloecryptSessionNonce
        session_signature: DilithiumSignature
        x_delegate_signer: XDilithiumSigner
        x_stable_kem: XKyberFullKEM
        x_session_kem: XKyberFullKEM
        x_stable_secret: XAloecryptSecret
        x_session_secret: XAloecryptSecret

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 34816

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(34816)
            off = 0
            assert len(self.nonce) == 32, f'nonce must be 32 bytes, got {len(self.nonce)}'
            buf.extend(self.nonce)
            assert len(self.session_signature) == 3309, f'session_signature must be 3309 bytes, got {len(self.session_signature)}'
            buf.extend(self.session_signature)
            buf.extend(self.x_delegate_signer.to_wire_bytes())
            buf.extend(self.x_stable_kem.to_wire_bytes())
            buf.extend(self.x_session_kem.to_wire_bytes())
            assert len(self.x_stable_secret) == 48, f'x_stable_secret must be 48 bytes, got {len(self.x_stable_secret)}'
            buf.extend(self.x_stable_secret)
            assert len(self.x_session_secret) == 48, f'x_session_secret must be 48 bytes, got {len(self.x_session_secret)}'
            buf.extend(self.x_session_secret)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'XParty':
            off = 0
            nonce = bytes(data[off:off+32]); off += 32
            session_signature = bytes(data[off:off+3309]); off += 3309
            x_delegate_signer = XDilithiumSigner.from_wire_bytes(data[off:off+9393]); off += 9393
            x_stable_kem = XKyberFullKEM.from_wire_bytes(data[off:off+10993]); off += 10993
            x_session_kem = XKyberFullKEM.from_wire_bytes(data[off:off+10993]); off += 10993
            x_stable_secret = bytes(data[off:off+48]); off += 48
            x_session_secret = bytes(data[off:off+48]); off += 48
            return cls(nonce=nonce, session_signature=session_signature, x_delegate_signer=x_delegate_signer, x_stable_kem=x_stable_kem, x_session_kem=x_session_kem, x_stable_secret=x_stable_secret, x_session_secret=x_session_secret)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 34816

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.nonce) == 32, f'nonce must be 32 bytes, got {len(self.nonce)}'
            buf.extend(self.nonce)
            assert len(self.session_signature) == 3309, f'session_signature must be 3309 bytes, got {len(self.session_signature)}'
            buf.extend(self.session_signature)
            buf.extend(self.x_delegate_signer.to_wire_bytes())
            buf.extend(self.x_stable_kem.to_wire_bytes())
            buf.extend(self.x_session_kem.to_wire_bytes())
            assert len(self.x_stable_secret) == 48, f'x_stable_secret must be 48 bytes, got {len(self.x_stable_secret)}'
            buf.extend(self.x_stable_secret)
            assert len(self.x_session_secret) == 48, f'x_session_secret must be 48 bytes, got {len(self.x_session_secret)}'
            buf.extend(self.x_session_secret)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'XParty':
            off = 0
            nonce = bytes(data[off:off+32]); off += 32
            session_signature = bytes(data[off:off+3309]); off += 3309
            x_delegate_signer = XDilithiumSigner.from_wire_bytes(data[off:off+9393]); off += 9393
            x_stable_kem = XKyberFullKEM.from_wire_bytes(data[off:off+10993]); off += 10993
            x_session_kem = XKyberFullKEM.from_wire_bytes(data[off:off+10993]); off += 10993
            x_stable_secret = bytes(data[off:off+48]); off += 48
            x_session_secret = bytes(data[off:off+48]); off += 48
            return cls(nonce=nonce, session_signature=session_signature, x_delegate_signer=x_delegate_signer, x_stable_kem=x_stable_kem, x_session_kem=x_session_kem, x_stable_secret=x_stable_secret, x_session_secret=x_session_secret)

    class XCounterParty(_PluginModel):
        address: AloecryptAddress
        nonce: AloecryptSessionNonce
        signature: DilithiumSignature
        stable_kem: KyberPublicKEM
        session_kem: KyberPublicKEM
        verifier: DilithiumVerifier
        x_stable_secret: XAloecryptSecret
        x_session_secret: XAloecryptSecret
        crypt_nonce: ChachaNonce

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 29752

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(29752)
            off = 0
            assert len(self.address) == 32, f'address must be 32 bytes, got {len(self.address)}'
            buf.extend(self.address)
            assert len(self.nonce) == 32, f'nonce must be 32 bytes, got {len(self.nonce)}'
            buf.extend(self.nonce)
            assert len(self.signature) == 3309, f'signature must be 3309 bytes, got {len(self.signature)}'
            buf.extend(self.signature)
            buf.extend(self.stable_kem.to_wire_bytes())
            buf.extend(self.session_kem.to_wire_bytes())
            buf.extend(self.verifier.to_wire_bytes())
            assert len(self.x_stable_secret) == 48, f'x_stable_secret must be 48 bytes, got {len(self.x_stable_secret)}'
            buf.extend(self.x_stable_secret)
            assert len(self.x_session_secret) == 48, f'x_session_secret must be 48 bytes, got {len(self.x_session_secret)}'
            buf.extend(self.x_session_secret)
            assert len(self.crypt_nonce) == 12, f'crypt_nonce must be 12 bytes, got {len(self.crypt_nonce)}'
            buf.extend(self.crypt_nonce)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'XCounterParty':
            off = 0
            address = bytes(data[off:off+32]); off += 32
            nonce = bytes(data[off:off+32]); off += 32
            signature = bytes(data[off:off+3309]); off += 3309
            stable_kem = KyberPublicKEM.from_wire_bytes(data[off:off+8501]); off += 8501
            session_kem = KyberPublicKEM.from_wire_bytes(data[off:off+8501]); off += 8501
            verifier = DilithiumVerifier.from_wire_bytes(data[off:off+9269]); off += 9269
            x_stable_secret = bytes(data[off:off+48]); off += 48
            x_session_secret = bytes(data[off:off+48]); off += 48
            crypt_nonce = bytes(data[off:off+12]); off += 12
            return cls(address=address, nonce=nonce, signature=signature, stable_kem=stable_kem, session_kem=session_kem, verifier=verifier, x_stable_secret=x_stable_secret, x_session_secret=x_session_secret, crypt_nonce=crypt_nonce)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 29752

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.address) == 32, f'address must be 32 bytes, got {len(self.address)}'
            buf.extend(self.address)
            assert len(self.nonce) == 32, f'nonce must be 32 bytes, got {len(self.nonce)}'
            buf.extend(self.nonce)
            assert len(self.signature) == 3309, f'signature must be 3309 bytes, got {len(self.signature)}'
            buf.extend(self.signature)
            buf.extend(self.stable_kem.to_wire_bytes())
            buf.extend(self.session_kem.to_wire_bytes())
            buf.extend(self.verifier.to_wire_bytes())
            assert len(self.x_stable_secret) == 48, f'x_stable_secret must be 48 bytes, got {len(self.x_stable_secret)}'
            buf.extend(self.x_stable_secret)
            assert len(self.x_session_secret) == 48, f'x_session_secret must be 48 bytes, got {len(self.x_session_secret)}'
            buf.extend(self.x_session_secret)
            assert len(self.crypt_nonce) == 12, f'crypt_nonce must be 12 bytes, got {len(self.crypt_nonce)}'
            buf.extend(self.crypt_nonce)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'XCounterParty':
            off = 0
            address = bytes(data[off:off+32]); off += 32
            nonce = bytes(data[off:off+32]); off += 32
            signature = bytes(data[off:off+3309]); off += 3309
            stable_kem = KyberPublicKEM.from_wire_bytes(data[off:off+8501]); off += 8501
            session_kem = KyberPublicKEM.from_wire_bytes(data[off:off+8501]); off += 8501
            verifier = DilithiumVerifier.from_wire_bytes(data[off:off+9269]); off += 9269
            x_stable_secret = bytes(data[off:off+48]); off += 48
            x_session_secret = bytes(data[off:off+48]); off += 48
            crypt_nonce = bytes(data[off:off+12]); off += 12
            return cls(address=address, nonce=nonce, signature=signature, stable_kem=stable_kem, session_kem=session_kem, verifier=verifier, x_stable_secret=x_stable_secret, x_session_secret=x_session_secret, crypt_nonce=crypt_nonce)


Party = party_api.Party
CounterParty = party_api.CounterParty
XParty = party_api.XParty
XCounterParty = party_api.XCounterParty

class session_api:
    class AloecryptSession(_PluginModel):
        party: Party
        counter_party: CounterParty
        session_salt: AloecryptSessionSalt

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 68280

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(68280)
            off = 0
            buf.extend(self.party.to_wire_bytes())
            buf.extend(self.counter_party.to_wire_bytes())
            assert len(self.session_salt) == 32, f'session_salt must be 32 bytes, got {len(self.session_salt)}'
            buf.extend(self.session_salt)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'AloecryptSession':
            off = 0
            party = Party.from_wire_bytes(data[off:off+38540]); off += 38540
            counter_party = CounterParty.from_wire_bytes(data[off:off+29708]); off += 29708
            session_salt = bytes(data[off:off+32]); off += 32
            return cls(party=party, counter_party=counter_party, session_salt=session_salt)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 68280

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            buf.extend(self.party.to_wire_bytes())
            buf.extend(self.counter_party.to_wire_bytes())
            assert len(self.session_salt) == 32, f'session_salt must be 32 bytes, got {len(self.session_salt)}'
            buf.extend(self.session_salt)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'AloecryptSession':
            off = 0
            party = Party.from_wire_bytes(data[off:off+38540]); off += 38540
            counter_party = CounterParty.from_wire_bytes(data[off:off+29708]); off += 29708
            session_salt = bytes(data[off:off+32]); off += 32
            return cls(party=party, counter_party=counter_party, session_salt=session_salt)

    class XAloecryptSession(_PluginModel):
        x_party: XParty
        x_counter_party: XCounterParty
        x_session_salt: XAloecryptSessionSalt
        un_hash: AloecryptHash
        priv_hash: AloecryptHash

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 64680

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(64680)
            off = 0
            buf.extend(self.x_party.to_wire_bytes())
            buf.extend(self.x_counter_party.to_wire_bytes())
            assert len(self.x_session_salt) == 48, f'x_session_salt must be 48 bytes, got {len(self.x_session_salt)}'
            buf.extend(self.x_session_salt)
            assert len(self.un_hash) == 32, f'un_hash must be 32 bytes, got {len(self.un_hash)}'
            buf.extend(self.un_hash)
            assert len(self.priv_hash) == 32, f'priv_hash must be 32 bytes, got {len(self.priv_hash)}'
            buf.extend(self.priv_hash)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'XAloecryptSession':
            off = 0
            x_party = XParty.from_wire_bytes(data[off:off+34816]); off += 34816
            x_counter_party = XCounterParty.from_wire_bytes(data[off:off+29752]); off += 29752
            x_session_salt = bytes(data[off:off+48]); off += 48
            un_hash = bytes(data[off:off+32]); off += 32
            priv_hash = bytes(data[off:off+32]); off += 32
            return cls(x_party=x_party, x_counter_party=x_counter_party, x_session_salt=x_session_salt, un_hash=un_hash, priv_hash=priv_hash)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 64680

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            buf.extend(self.x_party.to_wire_bytes())
            buf.extend(self.x_counter_party.to_wire_bytes())
            assert len(self.x_session_salt) == 48, f'x_session_salt must be 48 bytes, got {len(self.x_session_salt)}'
            buf.extend(self.x_session_salt)
            assert len(self.un_hash) == 32, f'un_hash must be 32 bytes, got {len(self.un_hash)}'
            buf.extend(self.un_hash)
            assert len(self.priv_hash) == 32, f'priv_hash must be 32 bytes, got {len(self.priv_hash)}'
            buf.extend(self.priv_hash)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'XAloecryptSession':
            off = 0
            x_party = XParty.from_wire_bytes(data[off:off+34816]); off += 34816
            x_counter_party = XCounterParty.from_wire_bytes(data[off:off+29752]); off += 29752
            x_session_salt = bytes(data[off:off+48]); off += 48
            un_hash = bytes(data[off:off+32]); off += 32
            priv_hash = bytes(data[off:off+32]); off += 32
            return cls(x_party=x_party, x_counter_party=x_counter_party, x_session_salt=x_session_salt, un_hash=un_hash, priv_hash=priv_hash)


AloecryptSession = session_api.AloecryptSession
XAloecryptSession = session_api.XAloecryptSession

class message_api:
    class MsgHELLO(_PluginModel):
        address: AloecryptAddress
        intro: PartyINTRO

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 26367

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(26367)
            off = 0
            assert len(self.address) == 32, f'address must be 32 bytes, got {len(self.address)}'
            buf.extend(self.address)
            buf.extend(self.intro.to_wire_bytes())
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgHELLO':
            off = 0
            address = bytes(data[off:off+32]); off += 32
            intro = PartyINTRO.from_wire_bytes(data[off:off+26335]); off += 26335
            return cls(address=address, intro=intro)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 26367

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.address) == 32, f'address must be 32 bytes, got {len(self.address)}'
            buf.extend(self.address)
            buf.extend(self.intro.to_wire_bytes())
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgHELLO':
            off = 0
            address = bytes(data[off:off+32]); off += 32
            intro = PartyINTRO.from_wire_bytes(data[off:off+26335]); off += 26335
            return cls(address=address, intro=intro)

    class MsgSYN(_PluginModel):
        syn_to: AloecryptSessionNonce
        syn_address: AloecryptAddress
        intro: PartyINTRO
        cipher: PartyCIPHER

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 31884

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(31884)
            off = 0
            assert len(self.syn_to) == 32, f'syn_to must be 32 bytes, got {len(self.syn_to)}'
            buf.extend(self.syn_to)
            assert len(self.syn_address) == 32, f'syn_address must be 32 bytes, got {len(self.syn_address)}'
            buf.extend(self.syn_address)
            buf.extend(self.intro.to_wire_bytes())
            buf.extend(self.cipher.to_wire_bytes())
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgSYN':
            off = 0
            syn_to = bytes(data[off:off+32]); off += 32
            syn_address = bytes(data[off:off+32]); off += 32
            intro = PartyINTRO.from_wire_bytes(data[off:off+26335]); off += 26335
            cipher = PartyCIPHER.from_wire_bytes(data[off:off+5485]); off += 5485
            return cls(syn_to=syn_to, syn_address=syn_address, intro=intro, cipher=cipher)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 31884

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.syn_to) == 32, f'syn_to must be 32 bytes, got {len(self.syn_to)}'
            buf.extend(self.syn_to)
            assert len(self.syn_address) == 32, f'syn_address must be 32 bytes, got {len(self.syn_address)}'
            buf.extend(self.syn_address)
            buf.extend(self.intro.to_wire_bytes())
            buf.extend(self.cipher.to_wire_bytes())
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgSYN':
            off = 0
            syn_to = bytes(data[off:off+32]); off += 32
            syn_address = bytes(data[off:off+32]); off += 32
            intro = PartyINTRO.from_wire_bytes(data[off:off+26335]); off += 26335
            cipher = PartyCIPHER.from_wire_bytes(data[off:off+5485]); off += 5485
            return cls(syn_to=syn_to, syn_address=syn_address, intro=intro, cipher=cipher)

    class MsgACK(_PluginModel):
        ack_to: AloecryptSessionNonce
        ack_address: AloecryptAddress
        cipher: PartyCIPHER
        challenge: PartyCHALLENGE

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 5677

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(5677)
            off = 0
            assert len(self.ack_to) == 32, f'ack_to must be 32 bytes, got {len(self.ack_to)}'
            buf.extend(self.ack_to)
            assert len(self.ack_address) == 32, f'ack_address must be 32 bytes, got {len(self.ack_address)}'
            buf.extend(self.ack_address)
            buf.extend(self.cipher.to_wire_bytes())
            buf.extend(self.challenge.to_wire_bytes())
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgACK':
            off = 0
            ack_to = bytes(data[off:off+32]); off += 32
            ack_address = bytes(data[off:off+32]); off += 32
            cipher = PartyCIPHER.from_wire_bytes(data[off:off+5485]); off += 5485
            challenge = PartyCHALLENGE.from_wire_bytes(data[off:off+128]); off += 128
            return cls(ack_to=ack_to, ack_address=ack_address, cipher=cipher, challenge=challenge)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 5677

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.ack_to) == 32, f'ack_to must be 32 bytes, got {len(self.ack_to)}'
            buf.extend(self.ack_to)
            assert len(self.ack_address) == 32, f'ack_address must be 32 bytes, got {len(self.ack_address)}'
            buf.extend(self.ack_address)
            buf.extend(self.cipher.to_wire_bytes())
            buf.extend(self.challenge.to_wire_bytes())
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgACK':
            off = 0
            ack_to = bytes(data[off:off+32]); off += 32
            ack_address = bytes(data[off:off+32]); off += 32
            cipher = PartyCIPHER.from_wire_bytes(data[off:off+5485]); off += 5485
            challenge = PartyCHALLENGE.from_wire_bytes(data[off:off+128]); off += 128
            return cls(ack_to=ack_to, ack_address=ack_address, cipher=cipher, challenge=challenge)

    class MsgSYNACK(_PluginModel):
        syn_ack: AloecryptSessionSalt
        challenge: PartyCHALLENGE
        challenge_response: PartyRESPONSE

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 192

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(192)
            off = 0
            assert len(self.syn_ack) == 32, f'syn_ack must be 32 bytes, got {len(self.syn_ack)}'
            buf.extend(self.syn_ack)
            buf.extend(self.challenge.to_wire_bytes())
            buf.extend(self.challenge_response.to_wire_bytes())
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgSYNACK':
            off = 0
            syn_ack = bytes(data[off:off+32]); off += 32
            challenge = PartyCHALLENGE.from_wire_bytes(data[off:off+128]); off += 128
            challenge_response = PartyRESPONSE.from_wire_bytes(data[off:off+32]); off += 32
            return cls(syn_ack=syn_ack, challenge=challenge, challenge_response=challenge_response)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 192

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.syn_ack) == 32, f'syn_ack must be 32 bytes, got {len(self.syn_ack)}'
            buf.extend(self.syn_ack)
            buf.extend(self.challenge.to_wire_bytes())
            buf.extend(self.challenge_response.to_wire_bytes())
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgSYNACK':
            off = 0
            syn_ack = bytes(data[off:off+32]); off += 32
            challenge = PartyCHALLENGE.from_wire_bytes(data[off:off+128]); off += 128
            challenge_response = PartyRESPONSE.from_wire_bytes(data[off:off+32]); off += 32
            return cls(syn_ack=syn_ack, challenge=challenge, challenge_response=challenge_response)

    class MsgWELCOME(_PluginModel):
        challenge_response: PartyRESPONSE

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 32

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(32)
            off = 0
            buf.extend(self.challenge_response.to_wire_bytes())
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgWELCOME':
            off = 0
            challenge_response = PartyRESPONSE.from_wire_bytes(data[off:off+32]); off += 32
            return cls(challenge_response=challenge_response)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 32

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            buf.extend(self.challenge_response.to_wire_bytes())
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgWELCOME':
            off = 0
            challenge_response = PartyRESPONSE.from_wire_bytes(data[off:off+32]); off += 32
            return cls(challenge_response=challenge_response)

    class MsgGOODBYE(_PluginModel):
        address: AloecryptAddress
        session_salt: AloecryptSessionSalt

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 64

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(64)
            off = 0
            assert len(self.address) == 32, f'address must be 32 bytes, got {len(self.address)}'
            buf.extend(self.address)
            assert len(self.session_salt) == 32, f'session_salt must be 32 bytes, got {len(self.session_salt)}'
            buf.extend(self.session_salt)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgGOODBYE':
            off = 0
            address = bytes(data[off:off+32]); off += 32
            session_salt = bytes(data[off:off+32]); off += 32
            return cls(address=address, session_salt=session_salt)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 64

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.address) == 32, f'address must be 32 bytes, got {len(self.address)}'
            buf.extend(self.address)
            assert len(self.session_salt) == 32, f'session_salt must be 32 bytes, got {len(self.session_salt)}'
            buf.extend(self.session_salt)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgGOODBYE':
            off = 0
            address = bytes(data[off:off+32]); off += 32
            session_salt = bytes(data[off:off+32]); off += 32
            return cls(address=address, session_salt=session_salt)

    class MsgTRANSPORT(_PluginModel):
        session_salt: AloecryptSessionSalt
        payload: bytes

        @classmethod
        def wire_byte_sz(cls) -> int:
            return None

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(None)
            off = 0
            assert len(self.session_salt) == 32, f'session_salt must be 32 bytes, got {len(self.session_salt)}'
            buf.extend(self.session_salt)
            buf.extend(len(self.payload).to_bytes(4, 'little'))
            buf.extend(self.payload)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgTRANSPORT':
            off = 0
            session_salt = bytes(data[off:off+32]); off += 32
            _vlen = int.from_bytes(data[off:off+4], 'little'); off += 4
            payload = bytes(data[off:off+_vlen]); off += _vlen
            return cls(session_salt=session_salt, payload=payload)

        @classmethod
        def wire_byte_sz(cls) -> int:
            raise NotImplementedError('MsgTRANSPORT has variable-length fields')

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.session_salt) == 32, f'session_salt must be 32 bytes, got {len(self.session_salt)}'
            buf.extend(self.session_salt)
            buf.extend(len(self.payload).to_bytes(4, 'little'))
            buf.extend(self.payload)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgTRANSPORT':
            off = 0
            session_salt = bytes(data[off:off+32]); off += 32
            _vlen = int.from_bytes(data[off:off+4], 'little'); off += 4
            payload = bytes(data[off:off+_vlen]); off += _vlen
            return cls(session_salt=session_salt, payload=payload)

    class MsgRETRY(_PluginModel):
        address: AloecryptAddress

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 32

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(32)
            off = 0
            assert len(self.address) == 32, f'address must be 32 bytes, got {len(self.address)}'
            buf.extend(self.address)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgRETRY':
            off = 0
            address = bytes(data[off:off+32]); off += 32
            return cls(address=address)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 32

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.address) == 32, f'address must be 32 bytes, got {len(self.address)}'
            buf.extend(self.address)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgRETRY':
            off = 0
            address = bytes(data[off:off+32]); off += 32
            return cls(address=address)

    class MsgROTATE(_PluginModel):
        session_salt: AloecryptSessionSalt
        syn_to: AloecryptSessionNonce
        syn_address: AloecryptAddress
        cipher: PartyCIPHER

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 5581

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(5581)
            off = 0
            assert len(self.session_salt) == 32, f'session_salt must be 32 bytes, got {len(self.session_salt)}'
            buf.extend(self.session_salt)
            assert len(self.syn_to) == 32, f'syn_to must be 32 bytes, got {len(self.syn_to)}'
            buf.extend(self.syn_to)
            assert len(self.syn_address) == 32, f'syn_address must be 32 bytes, got {len(self.syn_address)}'
            buf.extend(self.syn_address)
            buf.extend(self.cipher.to_wire_bytes())
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgROTATE':
            off = 0
            session_salt = bytes(data[off:off+32]); off += 32
            syn_to = bytes(data[off:off+32]); off += 32
            syn_address = bytes(data[off:off+32]); off += 32
            cipher = PartyCIPHER.from_wire_bytes(data[off:off+5485]); off += 5485
            return cls(session_salt=session_salt, syn_to=syn_to, syn_address=syn_address, cipher=cipher)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 5581

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.session_salt) == 32, f'session_salt must be 32 bytes, got {len(self.session_salt)}'
            buf.extend(self.session_salt)
            assert len(self.syn_to) == 32, f'syn_to must be 32 bytes, got {len(self.syn_to)}'
            buf.extend(self.syn_to)
            assert len(self.syn_address) == 32, f'syn_address must be 32 bytes, got {len(self.syn_address)}'
            buf.extend(self.syn_address)
            buf.extend(self.cipher.to_wire_bytes())
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgROTATE':
            off = 0
            session_salt = bytes(data[off:off+32]); off += 32
            syn_to = bytes(data[off:off+32]); off += 32
            syn_address = bytes(data[off:off+32]); off += 32
            cipher = PartyCIPHER.from_wire_bytes(data[off:off+5485]); off += 5485
            return cls(session_salt=session_salt, syn_to=syn_to, syn_address=syn_address, cipher=cipher)

    class MsgRESYN(_PluginModel):
        address: AloecryptAddress
        intro: PartyINTRO

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 26367

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(26367)
            off = 0
            assert len(self.address) == 32, f'address must be 32 bytes, got {len(self.address)}'
            buf.extend(self.address)
            buf.extend(self.intro.to_wire_bytes())
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgRESYN':
            off = 0
            address = bytes(data[off:off+32]); off += 32
            intro = PartyINTRO.from_wire_bytes(data[off:off+26335]); off += 26335
            return cls(address=address, intro=intro)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 26367

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.address) == 32, f'address must be 32 bytes, got {len(self.address)}'
            buf.extend(self.address)
            buf.extend(self.intro.to_wire_bytes())
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgRESYN':
            off = 0
            address = bytes(data[off:off+32]); off += 32
            intro = PartyINTRO.from_wire_bytes(data[off:off+26335]); off += 26335
            return cls(address=address, intro=intro)

    class MsgERROR(_PluginModel):
        address: AloecryptAddress

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 32

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(32)
            off = 0
            assert len(self.address) == 32, f'address must be 32 bytes, got {len(self.address)}'
            buf.extend(self.address)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgERROR':
            off = 0
            address = bytes(data[off:off+32]); off += 32
            return cls(address=address)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 32

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.address) == 32, f'address must be 32 bytes, got {len(self.address)}'
            buf.extend(self.address)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'MsgERROR':
            off = 0
            address = bytes(data[off:off+32]); off += 32
            return cls(address=address)


MsgHELLO = message_api.MsgHELLO
MsgSYN = message_api.MsgSYN
MsgACK = message_api.MsgACK
MsgSYNACK = message_api.MsgSYNACK
MsgWELCOME = message_api.MsgWELCOME
MsgGOODBYE = message_api.MsgGOODBYE
MsgTRANSPORT = message_api.MsgTRANSPORT
MsgRETRY = message_api.MsgRETRY
MsgROTATE = message_api.MsgROTATE
MsgRESYN = message_api.MsgRESYN
MsgERROR = message_api.MsgERROR

class builder_api:
    class PartyINTRO(_PluginModel):
        address: AloecryptAddress
        nonce: AloecryptSessionNonce
        stable_kem: KyberPublicKEM
        session_kem: KyberPublicKEM
        verifier: DilithiumVerifier

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 26335

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(26335)
            off = 0
            assert len(self.address) == 32, f'address must be 32 bytes, got {len(self.address)}'
            buf.extend(self.address)
            assert len(self.nonce) == 32, f'nonce must be 32 bytes, got {len(self.nonce)}'
            buf.extend(self.nonce)
            buf.extend(self.stable_kem.to_wire_bytes())
            buf.extend(self.session_kem.to_wire_bytes())
            buf.extend(self.verifier.to_wire_bytes())
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'PartyINTRO':
            off = 0
            address = bytes(data[off:off+32]); off += 32
            nonce = bytes(data[off:off+32]); off += 32
            stable_kem = KyberPublicKEM.from_wire_bytes(data[off:off+8501]); off += 8501
            session_kem = KyberPublicKEM.from_wire_bytes(data[off:off+8501]); off += 8501
            verifier = DilithiumVerifier.from_wire_bytes(data[off:off+9269]); off += 9269
            return cls(address=address, nonce=nonce, stable_kem=stable_kem, session_kem=session_kem, verifier=verifier)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 26335

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.address) == 32, f'address must be 32 bytes, got {len(self.address)}'
            buf.extend(self.address)
            assert len(self.nonce) == 32, f'nonce must be 32 bytes, got {len(self.nonce)}'
            buf.extend(self.nonce)
            buf.extend(self.stable_kem.to_wire_bytes())
            buf.extend(self.session_kem.to_wire_bytes())
            buf.extend(self.verifier.to_wire_bytes())
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'PartyINTRO':
            off = 0
            address = bytes(data[off:off+32]); off += 32
            nonce = bytes(data[off:off+32]); off += 32
            stable_kem = KyberPublicKEM.from_wire_bytes(data[off:off+8501]); off += 8501
            session_kem = KyberPublicKEM.from_wire_bytes(data[off:off+8501]); off += 8501
            verifier = DilithiumVerifier.from_wire_bytes(data[off:off+9269]); off += 9269
            return cls(address=address, nonce=nonce, stable_kem=stable_kem, session_kem=session_kem, verifier=verifier)

    class PartyCIPHER(_PluginModel):
        stable_cipher: KyberCipher
        session_cipher: KyberCipher
        signature: DilithiumSignature

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 5485

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(5485)
            off = 0
            assert len(self.stable_cipher) == 1088, f'stable_cipher must be 1088 bytes, got {len(self.stable_cipher)}'
            buf.extend(self.stable_cipher)
            assert len(self.session_cipher) == 1088, f'session_cipher must be 1088 bytes, got {len(self.session_cipher)}'
            buf.extend(self.session_cipher)
            assert len(self.signature) == 3309, f'signature must be 3309 bytes, got {len(self.signature)}'
            buf.extend(self.signature)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'PartyCIPHER':
            off = 0
            stable_cipher = bytes(data[off:off+1088]); off += 1088
            session_cipher = bytes(data[off:off+1088]); off += 1088
            signature = bytes(data[off:off+3309]); off += 3309
            return cls(stable_cipher=stable_cipher, session_cipher=session_cipher, signature=signature)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 5485

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.stable_cipher) == 1088, f'stable_cipher must be 1088 bytes, got {len(self.stable_cipher)}'
            buf.extend(self.stable_cipher)
            assert len(self.session_cipher) == 1088, f'session_cipher must be 1088 bytes, got {len(self.session_cipher)}'
            buf.extend(self.session_cipher)
            assert len(self.signature) == 3309, f'signature must be 3309 bytes, got {len(self.signature)}'
            buf.extend(self.signature)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'PartyCIPHER':
            off = 0
            stable_cipher = bytes(data[off:off+1088]); off += 1088
            session_cipher = bytes(data[off:off+1088]); off += 1088
            signature = bytes(data[off:off+3309]); off += 3309
            return cls(stable_cipher=stable_cipher, session_cipher=session_cipher, signature=signature)

    class FullCIPHER(_PluginModel):
        stable_cipher: KyberCipher
        session_cipher: KyberCipher
        stable_secret: AloecryptSecret
        session_secret: AloecryptSecret
        signature: DilithiumSignature

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 5549

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(5549)
            off = 0
            assert len(self.stable_cipher) == 1088, f'stable_cipher must be 1088 bytes, got {len(self.stable_cipher)}'
            buf.extend(self.stable_cipher)
            assert len(self.session_cipher) == 1088, f'session_cipher must be 1088 bytes, got {len(self.session_cipher)}'
            buf.extend(self.session_cipher)
            assert len(self.stable_secret) == 32, f'stable_secret must be 32 bytes, got {len(self.stable_secret)}'
            buf.extend(self.stable_secret)
            assert len(self.session_secret) == 32, f'session_secret must be 32 bytes, got {len(self.session_secret)}'
            buf.extend(self.session_secret)
            assert len(self.signature) == 3309, f'signature must be 3309 bytes, got {len(self.signature)}'
            buf.extend(self.signature)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'FullCIPHER':
            off = 0
            stable_cipher = bytes(data[off:off+1088]); off += 1088
            session_cipher = bytes(data[off:off+1088]); off += 1088
            stable_secret = bytes(data[off:off+32]); off += 32
            session_secret = bytes(data[off:off+32]); off += 32
            signature = bytes(data[off:off+3309]); off += 3309
            return cls(stable_cipher=stable_cipher, session_cipher=session_cipher, stable_secret=stable_secret, session_secret=session_secret, signature=signature)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 5549

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.stable_cipher) == 1088, f'stable_cipher must be 1088 bytes, got {len(self.stable_cipher)}'
            buf.extend(self.stable_cipher)
            assert len(self.session_cipher) == 1088, f'session_cipher must be 1088 bytes, got {len(self.session_cipher)}'
            buf.extend(self.session_cipher)
            assert len(self.stable_secret) == 32, f'stable_secret must be 32 bytes, got {len(self.stable_secret)}'
            buf.extend(self.stable_secret)
            assert len(self.session_secret) == 32, f'session_secret must be 32 bytes, got {len(self.session_secret)}'
            buf.extend(self.session_secret)
            assert len(self.signature) == 3309, f'signature must be 3309 bytes, got {len(self.signature)}'
            buf.extend(self.signature)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'FullCIPHER':
            off = 0
            stable_cipher = bytes(data[off:off+1088]); off += 1088
            session_cipher = bytes(data[off:off+1088]); off += 1088
            stable_secret = bytes(data[off:off+32]); off += 32
            session_secret = bytes(data[off:off+32]); off += 32
            signature = bytes(data[off:off+3309]); off += 3309
            return cls(stable_cipher=stable_cipher, session_cipher=session_cipher, stable_secret=stable_secret, session_secret=session_secret, signature=signature)

    class CounterPartySECRET(_PluginModel):
        stable_secret: AloecryptSecret
        session_secret: AloecryptSecret
        signature: DilithiumSignature

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 3373

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(3373)
            off = 0
            assert len(self.stable_secret) == 32, f'stable_secret must be 32 bytes, got {len(self.stable_secret)}'
            buf.extend(self.stable_secret)
            assert len(self.session_secret) == 32, f'session_secret must be 32 bytes, got {len(self.session_secret)}'
            buf.extend(self.session_secret)
            assert len(self.signature) == 3309, f'signature must be 3309 bytes, got {len(self.signature)}'
            buf.extend(self.signature)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'CounterPartySECRET':
            off = 0
            stable_secret = bytes(data[off:off+32]); off += 32
            session_secret = bytes(data[off:off+32]); off += 32
            signature = bytes(data[off:off+3309]); off += 3309
            return cls(stable_secret=stable_secret, session_secret=session_secret, signature=signature)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 3373

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.stable_secret) == 32, f'stable_secret must be 32 bytes, got {len(self.stable_secret)}'
            buf.extend(self.stable_secret)
            assert len(self.session_secret) == 32, f'session_secret must be 32 bytes, got {len(self.session_secret)}'
            buf.extend(self.session_secret)
            assert len(self.signature) == 3309, f'signature must be 3309 bytes, got {len(self.signature)}'
            buf.extend(self.signature)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'CounterPartySECRET':
            off = 0
            stable_secret = bytes(data[off:off+32]); off += 32
            session_secret = bytes(data[off:off+32]); off += 32
            signature = bytes(data[off:off+3309]); off += 3309
            return cls(stable_secret=stable_secret, session_secret=session_secret, signature=signature)

    class PartyCHALLENGE(_PluginModel):
        encrypted_challenge: XAloecryptSessionNonce
        encrypted_check: XAloecryptSessionNonce

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 128

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(128)
            off = 0
            assert len(self.encrypted_challenge) == 64, f'encrypted_challenge must be 64 bytes, got {len(self.encrypted_challenge)}'
            buf.extend(self.encrypted_challenge)
            assert len(self.encrypted_check) == 64, f'encrypted_check must be 64 bytes, got {len(self.encrypted_check)}'
            buf.extend(self.encrypted_check)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'PartyCHALLENGE':
            off = 0
            encrypted_challenge = bytes(data[off:off+64]); off += 64
            encrypted_check = bytes(data[off:off+64]); off += 64
            return cls(encrypted_challenge=encrypted_challenge, encrypted_check=encrypted_check)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 128

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.encrypted_challenge) == 64, f'encrypted_challenge must be 64 bytes, got {len(self.encrypted_challenge)}'
            buf.extend(self.encrypted_challenge)
            assert len(self.encrypted_check) == 64, f'encrypted_check must be 64 bytes, got {len(self.encrypted_check)}'
            buf.extend(self.encrypted_check)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'PartyCHALLENGE':
            off = 0
            encrypted_challenge = bytes(data[off:off+64]); off += 64
            encrypted_check = bytes(data[off:off+64]); off += 64
            return cls(encrypted_challenge=encrypted_challenge, encrypted_check=encrypted_check)

    class PartyRESPONSE(_PluginModel):
        decrypted_challenge: AloecryptSessionNonce

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 32

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(32)
            off = 0
            assert len(self.decrypted_challenge) == 32, f'decrypted_challenge must be 32 bytes, got {len(self.decrypted_challenge)}'
            buf.extend(self.decrypted_challenge)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'PartyRESPONSE':
            off = 0
            decrypted_challenge = bytes(data[off:off+32]); off += 32
            return cls(decrypted_challenge=decrypted_challenge)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 32

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.decrypted_challenge) == 32, f'decrypted_challenge must be 32 bytes, got {len(self.decrypted_challenge)}'
            buf.extend(self.decrypted_challenge)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'PartyRESPONSE':
            off = 0
            decrypted_challenge = bytes(data[off:off+32]); off += 32
            return cls(decrypted_challenge=decrypted_challenge)

    class CounterPartyCHALLENGE(_PluginModel):
        decrypted_challenge: AloecryptSessionNonce
        decrypted_check: AloecryptSessionNonce

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 64

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(64)
            off = 0
            assert len(self.decrypted_challenge) == 32, f'decrypted_challenge must be 32 bytes, got {len(self.decrypted_challenge)}'
            buf.extend(self.decrypted_challenge)
            assert len(self.decrypted_check) == 32, f'decrypted_check must be 32 bytes, got {len(self.decrypted_check)}'
            buf.extend(self.decrypted_check)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'CounterPartyCHALLENGE':
            off = 0
            decrypted_challenge = bytes(data[off:off+32]); off += 32
            decrypted_check = bytes(data[off:off+32]); off += 32
            return cls(decrypted_challenge=decrypted_challenge, decrypted_check=decrypted_check)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 64

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.decrypted_challenge) == 32, f'decrypted_challenge must be 32 bytes, got {len(self.decrypted_challenge)}'
            buf.extend(self.decrypted_challenge)
            assert len(self.decrypted_check) == 32, f'decrypted_check must be 32 bytes, got {len(self.decrypted_check)}'
            buf.extend(self.decrypted_check)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'CounterPartyCHALLENGE':
            off = 0
            decrypted_challenge = bytes(data[off:off+32]); off += 32
            decrypted_check = bytes(data[off:off+32]); off += 32
            return cls(decrypted_challenge=decrypted_challenge, decrypted_check=decrypted_check)

    class FromSecretsInput(_PluginModel):
        stable_secret_a: AloecryptSecret
        session_secret_a: AloecryptSecret
        signature_a: DilithiumSignature
        nonce_a: AloecryptSessionNonce
        address_a: AloecryptAddress
        stable_secret_b: AloecryptSecret
        session_secret_b: AloecryptSecret
        signature_b: DilithiumSignature
        nonce_b: AloecryptSessionNonce
        address_b: AloecryptAddress
        session_salt: AloecryptSessionSalt

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 6906

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(6906)
            off = 0
            assert len(self.stable_secret_a) == 32, f'stable_secret_a must be 32 bytes, got {len(self.stable_secret_a)}'
            buf.extend(self.stable_secret_a)
            assert len(self.session_secret_a) == 32, f'session_secret_a must be 32 bytes, got {len(self.session_secret_a)}'
            buf.extend(self.session_secret_a)
            assert len(self.signature_a) == 3309, f'signature_a must be 3309 bytes, got {len(self.signature_a)}'
            buf.extend(self.signature_a)
            assert len(self.nonce_a) == 32, f'nonce_a must be 32 bytes, got {len(self.nonce_a)}'
            buf.extend(self.nonce_a)
            assert len(self.address_a) == 32, f'address_a must be 32 bytes, got {len(self.address_a)}'
            buf.extend(self.address_a)
            assert len(self.stable_secret_b) == 32, f'stable_secret_b must be 32 bytes, got {len(self.stable_secret_b)}'
            buf.extend(self.stable_secret_b)
            assert len(self.session_secret_b) == 32, f'session_secret_b must be 32 bytes, got {len(self.session_secret_b)}'
            buf.extend(self.session_secret_b)
            assert len(self.signature_b) == 3309, f'signature_b must be 3309 bytes, got {len(self.signature_b)}'
            buf.extend(self.signature_b)
            assert len(self.nonce_b) == 32, f'nonce_b must be 32 bytes, got {len(self.nonce_b)}'
            buf.extend(self.nonce_b)
            assert len(self.address_b) == 32, f'address_b must be 32 bytes, got {len(self.address_b)}'
            buf.extend(self.address_b)
            assert len(self.session_salt) == 32, f'session_salt must be 32 bytes, got {len(self.session_salt)}'
            buf.extend(self.session_salt)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'FromSecretsInput':
            off = 0
            stable_secret_a = bytes(data[off:off+32]); off += 32
            session_secret_a = bytes(data[off:off+32]); off += 32
            signature_a = bytes(data[off:off+3309]); off += 3309
            nonce_a = bytes(data[off:off+32]); off += 32
            address_a = bytes(data[off:off+32]); off += 32
            stable_secret_b = bytes(data[off:off+32]); off += 32
            session_secret_b = bytes(data[off:off+32]); off += 32
            signature_b = bytes(data[off:off+3309]); off += 3309
            nonce_b = bytes(data[off:off+32]); off += 32
            address_b = bytes(data[off:off+32]); off += 32
            session_salt = bytes(data[off:off+32]); off += 32
            return cls(stable_secret_a=stable_secret_a, session_secret_a=session_secret_a, signature_a=signature_a, nonce_a=nonce_a, address_a=address_a, stable_secret_b=stable_secret_b, session_secret_b=session_secret_b, signature_b=signature_b, nonce_b=nonce_b, address_b=address_b, session_salt=session_salt)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 6906

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            assert len(self.stable_secret_a) == 32, f'stable_secret_a must be 32 bytes, got {len(self.stable_secret_a)}'
            buf.extend(self.stable_secret_a)
            assert len(self.session_secret_a) == 32, f'session_secret_a must be 32 bytes, got {len(self.session_secret_a)}'
            buf.extend(self.session_secret_a)
            assert len(self.signature_a) == 3309, f'signature_a must be 3309 bytes, got {len(self.signature_a)}'
            buf.extend(self.signature_a)
            assert len(self.nonce_a) == 32, f'nonce_a must be 32 bytes, got {len(self.nonce_a)}'
            buf.extend(self.nonce_a)
            assert len(self.address_a) == 32, f'address_a must be 32 bytes, got {len(self.address_a)}'
            buf.extend(self.address_a)
            assert len(self.stable_secret_b) == 32, f'stable_secret_b must be 32 bytes, got {len(self.stable_secret_b)}'
            buf.extend(self.stable_secret_b)
            assert len(self.session_secret_b) == 32, f'session_secret_b must be 32 bytes, got {len(self.session_secret_b)}'
            buf.extend(self.session_secret_b)
            assert len(self.signature_b) == 3309, f'signature_b must be 3309 bytes, got {len(self.signature_b)}'
            buf.extend(self.signature_b)
            assert len(self.nonce_b) == 32, f'nonce_b must be 32 bytes, got {len(self.nonce_b)}'
            buf.extend(self.nonce_b)
            assert len(self.address_b) == 32, f'address_b must be 32 bytes, got {len(self.address_b)}'
            buf.extend(self.address_b)
            assert len(self.session_salt) == 32, f'session_salt must be 32 bytes, got {len(self.session_salt)}'
            buf.extend(self.session_salt)
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'FromSecretsInput':
            off = 0
            stable_secret_a = bytes(data[off:off+32]); off += 32
            session_secret_a = bytes(data[off:off+32]); off += 32
            signature_a = bytes(data[off:off+3309]); off += 3309
            nonce_a = bytes(data[off:off+32]); off += 32
            address_a = bytes(data[off:off+32]); off += 32
            stable_secret_b = bytes(data[off:off+32]); off += 32
            session_secret_b = bytes(data[off:off+32]); off += 32
            signature_b = bytes(data[off:off+3309]); off += 3309
            nonce_b = bytes(data[off:off+32]); off += 32
            address_b = bytes(data[off:off+32]); off += 32
            session_salt = bytes(data[off:off+32]); off += 32
            return cls(stable_secret_a=stable_secret_a, session_secret_a=session_secret_a, signature_a=signature_a, nonce_a=nonce_a, address_a=address_a, stable_secret_b=stable_secret_b, session_secret_b=session_secret_b, signature_b=signature_b, nonce_b=nonce_b, address_b=address_b, session_salt=session_salt)

    class SessionBuilder(_PluginModel):
        delegate_signer: DilithiumSigner
        stable_kem: KyberFullKEM
        session_kem: KyberFullKEM
        nonce: AloecryptSessionNonce
        challenge_nonce: AloecryptSessionNonce
        session_salt: Optional[AloecryptSessionSalt] = None
        signature: Optional[DilithiumSignature] = None
        cipher: Optional[FullCIPHER] = None
        counterparty_intro: Optional[PartyINTRO] = None
        counterparty_cipher: Optional[CounterPartySECRET] = None
        counterparty_challenge: Optional[CounterPartyCHALLENGE] = None
        build_ready: bool

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 73868

        def to_wire_bytes(self) -> bytes:
            buf = bytearray(73868)
            off = 0
            buf.extend(self.delegate_signer.to_wire_bytes())
            buf.extend(self.stable_kem.to_wire_bytes())
            buf.extend(self.session_kem.to_wire_bytes())
            assert len(self.nonce) == 32, f'nonce must be 32 bytes, got {len(self.nonce)}'
            buf.extend(self.nonce)
            assert len(self.challenge_nonce) == 32, f'challenge_nonce must be 32 bytes, got {len(self.challenge_nonce)}'
            buf.extend(self.challenge_nonce)
            if self.session_salt is not None:
                buf[off] = 1; off += 1
                assert len(self.session_salt) == 32, f'session_salt must be 32 bytes, got {len(self.session_salt)}'
                buf.extend(self.session_salt)
            else:
                buf[off] = 0; off += 1 + 32
            if self.signature is not None:
                buf[off] = 1; off += 1
                assert len(self.signature) == 3309, f'signature must be 3309 bytes, got {len(self.signature)}'
                buf.extend(self.signature)
            else:
                buf[off] = 0; off += 1 + 3309
            if self.cipher is not None:
                buf[off] = 1; off += 1
                buf.extend(self.cipher.to_wire_bytes())
            else:
                buf[off] = 0; off += 1 + 5549
            if self.counterparty_intro is not None:
                buf[off] = 1; off += 1
                buf.extend(self.counterparty_intro.to_wire_bytes())
            else:
                buf[off] = 0; off += 1 + 26335
            if self.counterparty_cipher is not None:
                buf[off] = 1; off += 1
                buf.extend(self.counterparty_cipher.to_wire_bytes())
            else:
                buf[off] = 0; off += 1 + 3373
            if self.counterparty_challenge is not None:
                buf[off] = 1; off += 1
                buf.extend(self.counterparty_challenge.to_wire_bytes())
            else:
                buf[off] = 0; off += 1 + 64
            buf.append(int(self.build_ready))
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'SessionBuilder':
            off = 0
            delegate_signer = DilithiumSigner.from_wire_bytes(data[off:off+13333]); off += 13333
            stable_kem = KyberFullKEM.from_wire_bytes(data[off:off+10901]); off += 10901
            session_kem = KyberFullKEM.from_wire_bytes(data[off:off+10901]); off += 10901
            nonce = bytes(data[off:off+32]); off += 32
            challenge_nonce = bytes(data[off:off+32]); off += 32
            if data[off] == 1:
                off += 1
                session_salt = bytes(data[off:off+32]); off += 32
            else:
                off += 1 + 32
                session_salt = None
            if data[off] == 1:
                off += 1
                signature = bytes(data[off:off+3309]); off += 3309
            else:
                off += 1 + 3309
                signature = None
            if data[off] == 1:
                off += 1
                cipher = FullCIPHER.from_wire_bytes(data[off:off+5549]); off += 5549
            else:
                off += 1 + 5549
                cipher = None
            if data[off] == 1:
                off += 1
                counterparty_intro = PartyINTRO.from_wire_bytes(data[off:off+26335]); off += 26335
            else:
                off += 1 + 26335
                counterparty_intro = None
            if data[off] == 1:
                off += 1
                counterparty_cipher = CounterPartySECRET.from_wire_bytes(data[off:off+3373]); off += 3373
            else:
                off += 1 + 3373
                counterparty_cipher = None
            if data[off] == 1:
                off += 1
                counterparty_challenge = CounterPartyCHALLENGE.from_wire_bytes(data[off:off+64]); off += 64
            else:
                off += 1 + 64
                counterparty_challenge = None
            build_ready = bool(data[off]); off += 1
            return cls(delegate_signer=delegate_signer, stable_kem=stable_kem, session_kem=session_kem, nonce=nonce, challenge_nonce=challenge_nonce, session_salt=session_salt, signature=signature, cipher=cipher, counterparty_intro=counterparty_intro, counterparty_cipher=counterparty_cipher, counterparty_challenge=counterparty_challenge, build_ready=build_ready)

        @classmethod
        def wire_byte_sz(cls) -> int:
            return 73868

        def to_wire_bytes(self) -> bytes:
            buf = bytearray()
            buf.extend(self.delegate_signer.to_wire_bytes())
            buf.extend(self.stable_kem.to_wire_bytes())
            buf.extend(self.session_kem.to_wire_bytes())
            assert len(self.nonce) == 32, f'nonce must be 32 bytes, got {len(self.nonce)}'
            buf.extend(self.nonce)
            assert len(self.challenge_nonce) == 32, f'challenge_nonce must be 32 bytes, got {len(self.challenge_nonce)}'
            buf.extend(self.challenge_nonce)
            if self.session_salt is not None:
                buf.append(1)
                assert len(self.session_salt) == 32, f'session_salt must be 32 bytes, got {len(self.session_salt)}'
                buf.extend(self.session_salt)
            else:
                buf.append(0)
                buf.extend(b'\x00' * 32)
            if self.signature is not None:
                buf.append(1)
                assert len(self.signature) == 3309, f'signature must be 3309 bytes, got {len(self.signature)}'
                buf.extend(self.signature)
            else:
                buf.append(0)
                buf.extend(b'\x00' * 3309)
            if self.cipher is not None:
                buf.append(1)
                buf.extend(self.cipher.to_wire_bytes())
            else:
                buf.append(0)
                buf.extend(b'\x00' * 5549)
            if self.counterparty_intro is not None:
                buf.append(1)
                buf.extend(self.counterparty_intro.to_wire_bytes())
            else:
                buf.append(0)
                buf.extend(b'\x00' * 26335)
            if self.counterparty_cipher is not None:
                buf.append(1)
                buf.extend(self.counterparty_cipher.to_wire_bytes())
            else:
                buf.append(0)
                buf.extend(b'\x00' * 3373)
            if self.counterparty_challenge is not None:
                buf.append(1)
                buf.extend(self.counterparty_challenge.to_wire_bytes())
            else:
                buf.append(0)
                buf.extend(b'\x00' * 64)
            buf.append(int(self.build_ready))
            return bytes(buf)

        @classmethod
        def from_wire_bytes(cls, data: bytes) -> 'SessionBuilder':
            off = 0
            delegate_signer = DilithiumSigner.from_wire_bytes(data[off:off+13333]); off += 13333
            stable_kem = KyberFullKEM.from_wire_bytes(data[off:off+10901]); off += 10901
            session_kem = KyberFullKEM.from_wire_bytes(data[off:off+10901]); off += 10901
            nonce = bytes(data[off:off+32]); off += 32
            challenge_nonce = bytes(data[off:off+32]); off += 32
            if data[off] == 1:
                off += 1
                session_salt = bytes(data[off:off+32]); off += 32
            else:
                off += 1 + 32
                session_salt = None
            if data[off] == 1:
                off += 1
                signature = bytes(data[off:off+3309]); off += 3309
            else:
                off += 1 + 3309
                signature = None
            if data[off] == 1:
                off += 1
                cipher = FullCIPHER.from_wire_bytes(data[off:off+5549]); off += 5549
            else:
                off += 1 + 5549
                cipher = None
            if data[off] == 1:
                off += 1
                counterparty_intro = PartyINTRO.from_wire_bytes(data[off:off+26335]); off += 26335
            else:
                off += 1 + 26335
                counterparty_intro = None
            if data[off] == 1:
                off += 1
                counterparty_cipher = CounterPartySECRET.from_wire_bytes(data[off:off+3373]); off += 3373
            else:
                off += 1 + 3373
                counterparty_cipher = None
            if data[off] == 1:
                off += 1
                counterparty_challenge = CounterPartyCHALLENGE.from_wire_bytes(data[off:off+64]); off += 64
            else:
                off += 1 + 64
                counterparty_challenge = None
            build_ready = bool(data[off]); off += 1
            return cls(delegate_signer=delegate_signer, stable_kem=stable_kem, session_kem=session_kem, nonce=nonce, challenge_nonce=challenge_nonce, session_salt=session_salt, signature=signature, cipher=cipher, counterparty_intro=counterparty_intro, counterparty_cipher=counterparty_cipher, counterparty_challenge=counterparty_challenge, build_ready=build_ready)


PartyINTRO = builder_api.PartyINTRO
PartyCIPHER = builder_api.PartyCIPHER
FullCIPHER = builder_api.FullCIPHER
CounterPartySECRET = builder_api.CounterPartySECRET
PartyCHALLENGE = builder_api.PartyCHALLENGE
PartyRESPONSE = builder_api.PartyRESPONSE
CounterPartyCHALLENGE = builder_api.CounterPartyCHALLENGE
FromSecretsInput = builder_api.FromSecretsInput
SessionBuilder = builder_api.SessionBuilder
